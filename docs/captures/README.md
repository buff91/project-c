# 검증 참고 캡처

Claude/Codex가 구현 상태와 화면 회귀를 비교할 때 보는 캡처를 둔다.

- Unity 씬·프리팹·ScriptableObject가 참조하는 런타임 자산은 이곳에 두지 않는다.
- MCP 캡처가 `Assets/Screenshots/`에 생성되면 검토 후 보관할 파일만 이곳으로 옮긴다.
- `docs` 아래 파일은 Unity가 임포트하지 않으므로 `.meta`를 만들거나 보관하지 않는다.
