# 환경 6셀 소스 시트 v4 — 클린 클러스터 재조정 (§1-d)

- **채택**: 2026-07-30, art_runner job `ART-20260730-041106-2f52aa` 후보 **C03**.
  v3(같은 날 채택)를 §1-d 렌더링 문법 계약 도입으로 즉시 개정 — 잔점 노이즈 사실계를
  버리고 플랫 면 + 이음새·해치 재질감으로 재생성했다. 레시피
  `environment-neon-style-v1`의 클린 클러스터 재조정판(PixelArtRedmond 0.55,
  denoise 0.55, speckle 계열 negative).
- **판정**: 신설 게이트 — 후보 바닥 셀 3×3 위 라이더 스프라이트 합성으로
  캐릭터-환경 문법 정합을 확인하고 채택(§1-d).
- **전처리**: 생성 배경이 불투명 검정이라 테두리 연결 플러드(tol 5)로 알파화 후 저장.
