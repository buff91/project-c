# 로드맵 & 진행 상태

> 원칙: **각 마일스톤은 "플레이 가능한" 상태로 끝난다.** 앞 단계 없이 뒷 단계로 점프 금지.
> 진행 상태의 SSOT는 이 파일이다 — `GDD.md` §8은 최초 범위 정의를 보존한 역사적 기준선일 뿐
> 현재 상태가 아니다(설계 근거는 GDD §5·§10). **완료 이력은
> [`ROADMAP-ARCHIVE.md`](ROADMAP-ARCHIVE.md)로 분리했다**(M0~M6 + 병합분 · 첫 던전 구간별 정체성 ·
> 전투 깊이 · 해금 축 · 지역 정체성 · 폐병원/상승 전환 · 3D 시야선 3단계). 완료 항목이 쌓이기만 해서
> "무엇을 다음에 하나"가 안 읽혔고, 그게 이 문서가 존재하는 이유였다.

## 다음 할 일

> 미완(`[ ]`) 항목 전부를 여기 모은다 — 배경은 아카이브의 같은 이름 절에 있다.
> **코드 쪽 미완은 본문을 아래 절이 진다**: `IsoPrototypeDemo` 2단계·공용 Tween·DOTween
> 결정 → 「리팩토링」, 정리 후보 3건 → 「백로그」, 생성기 BSP 3건·입체 전투 → 「향후 기술 과제」 두 절.

### 콘텐츠 & 밸런스 — 실플레이 리포트가 선행한다

- [ ] **장비 확장** — 조합 재료를 쓰는 제작(GDD §5.6 "조합으로 장비 제작"), 갈고리/등불 같은
  이동·시야 장비, 던전에서 줍는 장비. 지금은 골드 제작 4종만 있다.
- [ ] **플레이어 원거리 재조정** — 무제한 무료 원거리는 M4에서 충전형 2티어로 갈렸다
  (내장 이미터 3/2/6턴 · 아크 캐스터 5/4/4턴 · 에너지 셀 = 즉시 만충).
  남은 것은 수치다. **조정 축은 판당 사격 횟수 하나** — 재충전이 빠르면 옛 무료 원거리로,
  느리면 죽은 선택지로 간다. 리포트에서 그 값을 먼저 보고 재충전 턴부터 만진다.
  피해 1·제작비 145G·조합비(코어 파편 1 + 뇌관 화약 1)도 임시값이다.
- [ ] **해금 조건·구출 층 수치 조정** — 화상 12·처치 20 같은 조건값과 NPC 구출 층(2·5)은
  실플레이 전 임시값이다. 코드는 완료됐고 남은 것은 숫자뿐이다.
- [ ] **지역별 의뢰** — `BountyMetric`에 이미 `WaterFrozen`/`OilIgnited`가 있어 "왜 저기 가는가"를
  붙일 수 있다.
- [ ] **쉘터 재건·성장** — 판을 거듭하며 시설이 복구되는 축(포스트아포칼립스 정착지).
  해금 축 B단계(NPC 구출 → 시설 해금)가 이것의 첫 실체화다.
- [ ] **사운드** (마지막으로 연기)

### 아트

- [x] **로컬 생성·리뷰 자동화 기반** — ComfyUI REST 레시피/큐, 샷별 Aseprite conform,
  Lua 애니메이션 초안(`idle/walk/attack/hit/fall/death`, `burst/idle-loop`), Slack
  Socket Mode 검수와 launchd 상시 실행까지 연결했다. 수동 다용도 배치·대기 취소·실패 재시도,
  승인 스냅샷과 Spark 게임 반영 요청 큐도 같은 SQLite에 연결했다. 실제 정식 자산 생성·채택은
  아래 배치가 소유한다. Unity 쪽도 Source/Aseprite 원본 저장 → 임포트 규격 적용 → 후처리
  카탈로그/애니메이션 세트 동기화까지 자동이다. 수동 `Reimport and Sync Catalog`는 자동 새로고침이
  꺼졌거나 임포트 규칙을 소급 적용할 때의 복구 경로다. 초기 `e0c0967`의 28개 기준에서 확장되어,
  2026-08-02 작업 트리 검증에서 Aseprite 원본 61개 / 카탈로그 슬롯 61개가
  `Validate Sources`를 통과했다. 2026-08-03 작업 트리에서는 원정자 방향 원본과 신규 액터 소스를
  포함한 63개 원본이 다시 통과했다.
- [ ] **환경 네온 전환(M5 — 네온 아케이드 재발주)** — v0.3.3 재설정에 따른 환경 자산 전환의
  소유 항목이다(아카이브의 구 "M5 — 콘텐츠 & 폴리시"와는 무관한 별개 이름이다).
  - [x] **1차 슬라이스(2026-07-30)**: 공용 환경 6셀(바닥·좌우 벽·문 2종·계단)을
    `environment-neon-style-v1` 레시피(C03 채택)로 재마감해 `Art/Environment` 16종 교체,
    원정자는 치비 라이더로 교체. 캡처 `docs/captures/neon-environment-live-v1.png`.
  - [x] **2차 슬라이스(2026-07-30)**: `hospital*` 드레싱 9슬롯(바닥 3 + 상승 벽 3×좌우)을
    `environment-neon-dressing-v1` 레시피(C03 채택, §1-d 라이더 합성 게이트)로 아케이드
    어휘(자판기·꺼진 홀로 패널·상태 패널 + 그레이트/전단지 균열/서비스 패널 바닥)로 교체 —
    슬롯명·파일명 계약은 구판 유지. 캡처 `docs/captures/arcade-dressing-live-v1.png`.
  - [x] **3차 슬라이스(2026-07-30)**: 기존 소품 4종(모닥불·폭발 배럴·포탈·은닉처)을
    `arcade-props-neon-v1` 레시피(C04 채택)로 §1-d 플랫 클러스터 문법 재마감 — 실루엣·
    역할색(토치 골드/틸/hazard) 보존, `Art/Runtime/prop-*.png` 교체. 같은 날 드레싱
    정합 패스(바닥 명도 하향, 벽은 쿨 grey-* 보존)도 프로세서에 반영. 캡처
    `docs/captures/arcade-dressing-live-v3.png` · `arcade-props-hub-live-v1.png`.
  - [x] **4차 슬라이스(2026-07-30)**: 하행 계단 특수 소스를 `environment-neon-stairs-v1`
    레시피(C04 채택 — C01은 피트 틸 광이 Hole 신호와 충돌해 기각)로 교체,
    `env-stairs-down-rising-*` 2종 재마감. 마지막 병원판 환경 소스가 소거됐다.
    판정 근거 `docs/captures/arcade-stairs-conform-v1.png`,
    출처 `project-c-arcade-stairs-source-v1.prompt.md`.
  - [x] **5차 슬라이스(2026-07-30)**: 메인 메뉴 배경을 `ui-menu-backdrop-v2` 레시피
    (C02 채택, job ART-20260730-070942-2c2580)의 네온 스카이라인으로 교체 —
    구 병원 복도 소스(`project-c-main-menu-backdrop-source-v1`)를 대체해
    **마지막 구테마 유저 노출 자산이 소거됐다. M5 재발주 범위 종료.**
  - [x] **B2 바닥 드레싱 시점 정합(2026-08-01 작업 트리)**: 주차 범퍼·쓰러진 안내판에
    각각 `view-0..3` 네 방향 Aseprite/카탈로그 슬롯을 연결하고 현재 시점 회전으로 선택한다.
    방향형은 네 슬롯 완전 세트에서만 켜고, 부분 승격이면 기존 무방향 슬롯을 전 시점에 쓰며,
    그것도 없으면 같은 화면축 parity → 첫 존재 슬롯 순으로 내려가 장식이 사라지지 않는다.
    **v2 후속 마감(2026-08-02)**에서 mirror 반복을 실제 4시점 소스로 교체하고, 범퍼는 낮은
    고무/강철, 안내판은 거의 눕힌 파손 판재로 분리했다. 완성형 바닥의 중성화+런타임 톤매핑으로
    베이지 섬을 제거했으며 q0..q3 근거는 `docs/captures/b2-right-dressing-*-v2.png`다.
  - [x] **B2 진출 균열 바닥(2026-08-02 작업 트리)**: 전역 구판 cracked를 B2 전용
    `env-floor-b2-cracked` Aseprite로 교체했다. 공용 바닥의 평면 실루엣을 유지하면서 작은 박리·
    가는 균열·극소량 녹만 합성해 높이·구멍·커버·판정 신호로 오독되지 않게 했다. 방향 seam이
    없어 단일 슬롯을 네 시점에 공유하며, 배치와 모든 게임 규칙은 그대로다. q0..q3 근거는
    `docs/captures/b2-cracked-floor-q{0,1,2,3}-live-v1.png`다.
  - [x] **B2 연속 서비스 벽(2026-08-02 작업 트리)**: 시작방 물리 -Y 벽 중앙 세 칸을
    방향별 한 master에서 자른 3세그먼트×좌우 Aseprite 완전 세트로 승격했다. 셀별 FOV·정렬·
    가림은 유지하고, 중앙 sconce의 실제 앰버 광원만 남긴다. 불완전 세트는 세 칸 전체 폴백.
    q0/q1 실화면과 q2 반대편 누출 검사를 `docs/captures/b2-service-wall-*-v1.png`에 보관했다.
  - [x] **B2 벽체 seam·바닥 경계·접합 리듬 교정(2026-08-03 작업 트리)**: 64×112 벽 전종을 공통
    아이소 알파 계약으로 다시 마감해 64×32px 인접 배치에서 상단 캡·하부 받침이 끊기지 않게 했다.
    서비스 벽은 연결 master에서 최종 분할하고, 런타임 벽 피벗은 타일 경계의 정확한 중점에 둔다.
    독립 카드용 밝은 end-cap은 12px 저채도 공통 접합주로 정리하고, 2px cap과 어두운 연속 plinth로
    반복 울타리·점선형 발선을 줄였다. 피벗·PPU·스케일·정렬과 게임 규칙은 유지하며 q0..q3 근거는
    `docs/captures/b2-wall-joinery-q{0,1,2,3}-live-v5.png`다.
  - [x] **B2 배럴 유출 방지 베이(2026-08-02 작업 트리)**: 폭발통 호스트 service/ring 셀과
    인접 drain/grate 셀을 2셀×4시점 Aseprite 완전 세트로 승격했다. 시점별 master-first 마감으로
    공유 호스·배수 띠를 잇고, 배럴의 이동·폭발 및 바닥 충돌/FOV는 바꾸지 않았다. 인접 sconce는
    실제 앰버 광원을 유지한 채 한 물리 벽 면에만 낮은 강조로 남긴다.
  - [x] **B2 2×2 연속 바닥 Macro(2026-08-02 작업 트리)**: 기본 seed의
    `(3,1)·(4,1)·(3,2)·(4,2)` 네 일반 바닥을 하나의 top-down master에서 파생한
    4역할×4시점 Aseprite 완전 세트로 승격했다. 타이어/오일 마모와 끊긴 주차선이 셀 경계를 넘어
    이어지고, 특수 드레싱·clear spine을 피한 clean 블록과 16슬롯이 모두 있을 때만 네 셀이
    원자적으로 켜진다. 이동 격자·높이·공격·FOV는 불변이며 q0..q3 근거는
    `docs/captures/b2-macro-floor-q{0,1,2,3}-live-v1.png`다.
  - [x] **B2 방 단위 바닥 조명(2026-08-02 작업 트리)**: 타일별 `SpriteRenderer.color`가
    플레이어·sconce·방향 그림자의 실측 0.585~0.994 광량을 큰 다이아 단위로 끊어 체크무늬를 만들던 원인을
    분리했다. 보이는 시작방 Floor의 평균광은 유지하고 local 차이만 20% 남기는 presentation 패스를
    바닥에만 적용했다. 2026-08-04에는 활성 층의 보이는 `Floor`를 Door·Stairs로 분리한 4방향 연결
    영역별로 같은 20% 보정을 일반화해 상층 체크무늬도 닫았다. 벽·적 액터·소품의 국소광과
    FOV·wet/oiled·이동/공격 표시는 그대로다. q0..q3 근거와 상층 근거는 각각
    `b2-floor-light-coherence-q{0,1,2,3}-live-v1.png`와 `floor-band-coherence-6f-pc-2026-08-04.png`다.
  - [x] **원정자·마커 접지 v1(2026-08-02 작업 트리)**: 메인 원정자를 하드 알파·24색 역할
    팔레트·2×2 클러스터의 `96×128` 단일 `Frame_0` Aseprite로 교체했다. 플레이어 안정색에
    상태×고도×local light를 적용하고 접촉 AO를 두 발 사이로 줄였다. `marker-player`와
    `marker-target`도 각각 틸/앰버 열린 코너 틱의 정식 Aseprite로 승격했다. 이 단계에서는
    애니메이션 승인 게이트를 닫았고, 아래 2026-08-03 방향 타임라인 항목에서 별도로 열었다.
  - [x] **B2 바닥 foundation 접지 v1(2026-08-02 작업 트리)**: 별도 비게임플레이 루트가 실제
    화면 전면에만 연속 10px fascia와 드문 이음매를 그리고, 월드의 같은 볼록 모서리에 고정된
    지지대를 앞면에만 표시한다. `Dungeon Backdrop` order 1/2를 사용하며 collider·입력·격자·FOV·
    전투는 불변이다. q0..q3 근거는 `docs/captures/foundation-grounding-q{0,1,2,3}-live-v2.png`다.
  - [x] **공용 바닥 재료 품질 v1(2026-08-02 작업 트리)**: built-in ImageGen 재료 소스를
    `process_shared_floor_material_v1.py`로 기존 128×64·4,098px hard-alpha 다이아에 다시 마감했다.
    원본은 `grey-4` 91.996%와 세 개의 넓은 마모 덩어리 `grey-3` 8.004%만 사용하고, 둘 다 런타임
    `Stone` 한 역할로 매핑해 타일마다 Shadow/Light/Outline 무늬가 찍히지 않게 했다. 공용 바닥을
    굽는 밴드·Facility·B2 파생 자산도 모두 재생성했다. 실루엣·피벗·격자·이동·공격·FOV는
    불변이며 q0..q3 근거는 `docs/captures/shared-floor-material-q{0,1,2,3}-live-v1.png`다.
  - [x] **기본 벽 재료 품질 재제작(2026-08-03 작업 트리)**: ImageGen 저주파 재질 소스를
    `process_b2_prop_quality_v4.py`로 기존 64×112 벽 셸에 다시 마감했다. 조용한 기본 셸과
    legacy window 슬롯을 재사용한 비발광 유지보수 셸을 만들고, B2의 비기능 벽 bay에는 물리
    좌표의 홀짝으로 고정 배치한다. 서비스 세그먼트·설비·단말의 우선순위와 방향·피벗·정렬·벽 배치·
    충돌·FOV·이동·공격 규칙은 불변이다. 실기 근거는
    `docs/captures/b2-wall-material-q{0,1,2,3}-live-v1.png`다.
- [x] **UI v1.8 쿨 전환 + Field Deck v1 — PC 화면 확인 완료**
  - 코드·자산은 반영됐다: 코어 계조 쿨 이동 · 네온 용도 세 갈래(시안=월드 장식 / 마젠타=UI 크롬 /
    틸=판정 신호) · `ui-*.png` 9종과 9-slice 재생성 · 코너 브래킷 `ui-bracket-frame.png` 신설.
    규칙 SSOT 는 `docs/UI_DESIGN_SYSTEM.md` v1.8, 스냅샷은 `docs/STATUS.md`.
  - [x] **Field Deck PC 1차 구현(2026-08-03 작업 트리)** — Mobile/Desktop wrapper 배선을 복구하고,
    분절 HP · 우상 176×104 단일 계기 · 208×52 로그 · 184/268px 고정폭 하단 두 레일 · 12×12 네이티브 glyph ·
    전체 footprint 액션 휠 안전 배치를 safe-area 로컬 좌표로 반영했다. 입장/발견 카드는 FIFO로 보존하고 보스가 같은 슬롯에서
    우선한다. 상단 세 구역은 바이탈 160px / 과도 패널 256px / 계기 176px로 서로 겹치지 않는다.
  - [x] **PC Game View 캡처 검증(2026-08-03)** — 2560×1440 Game View(논리 640×360)에서 정상 HUD,
    발견 카드+액션 휠, 실제 8F 보스+액션 휠을 각각 확인했다. 브래킷과 12px glyph가 읽히고 긴 문맥
    액션도 셀 안에 남으며, 과도 패널과 여섯 휠 셀의 교차 면적은 모두 0이다. 월드 라벨·네온이
    플레이트를 뚫고 보이던 마지막 결함은 상시 플레이트 96% 차폐, 보스/발견 플레이트 완전 차폐로
    닫았다. 기준본은 `docs/captures/ui-field-deck-pc-qhd-2026-08-03.png`와
    `docs/captures/ui-field-deck-{notice-wheel,boss-wheel}-pc-qhd-2026-08-03.png`다.
  - [x] **후속 PC UI 재캡처/전체 Unity 회귀(2026-08-03 작업 트리)** — 기본 상태에서 액션 휠이 닫히고,
    발견 카드 `×`와 미니맵 층/N/현재 위치 마커가 읽히는 것을
    `docs/captures/project-c-followup-{ui,discovery}-pc-qhd-2026-08-03.png`에서 확인했다. 같은 작업 트리의
    전체 EditMode/PlayMode 회귀와 스크립트 컴파일 오류 검사도 통과했다. 보행/층 전환 시간 연출은 정적 UI
    캡처가 아닌 정상 진행 플레이테스트에서 승인하고, 적 6종의 실제 PC 배율은 아래 배치 ② 방향 타임라인에서 닫았다.
  - [x] **PC 아이콘 경로 결정** — Desktop은 축소 재가공을 중단하고 `ui-field-*.png` 네이티브 12×12
    glyph 9종을 쓴다. 웜 소스→`cool_shift()` 32px 세트는 Mobile의 기존 배치용으로 유지하므로
    재발주나 마감 패스 삭제를 이번 범위에 넣지 않는다.
  - [x] **브래킷 적용 범위 결정** — PC 던전의 바이탈·우상 계기·보스/발견·로그·자원/행동 레일까지
    같은 `.pc-plate`로 묶는다. 허브·메인 메뉴·모달과 Mobile에는 확대 적용하지 않는다.
- [ ] **아케이드 소품 추가** (v0.3.3 개정 — 구 "병원 소품" 발주는 집행 전 폐기) — 자판기·네온 간판·
  홀로 패널·셔터 내린 점포. 확정된 환경 세트(콘크리트·비상등·방폭문·서비스 계단)는 그대로 쓰고
  **지상층부터 덧붙인다**(리스킨 표 §5 주석). 기존 `hospitalFloor*`/`hospitalWall*` 9슬롯은
  슬롯명 유지, 위 M5 재발주에서 아케이드 어휘로 교체.
- [ ] **아트풍 개선 2차 — ComfyUI 생성 배치 1~4** (플랜 v2 §3): ① 깊이 밴드 바닥 6종과
  hole/weak-floor/ladder(층간 엘리베이터·후속 던전 겸용) ② 액터 96×128 재생성 +
  `actor-arc-drone`/`actor-grave-warden` 전용 아트
  - [x] **배치 ① 깊이 밴드 바닥 6종(2026-07-30)**: `environment-band-floors-v1`
    (2라운드 C04 채택 — 1라운드는 색 드리프트·밴드 식별 실패로 폐기)로
    `env-floor-{mid,deep,boss}(-raised)` 마감, 카탈로그 밴드 슬롯 연결로 절차
    `BandOverlayColor` 임시 대행 자동 비활성. 2026-08-04 반복 안전 패스에서 생성 색·알파·긴 균열을
    직접 복사하던 conform을 폐기하고 공용 canonical 다이아 + `grey-2/3/4` 2×2 희소 디테일로
    재마감했다. 런타임은 전용 손상 타일을 약 10~13%의 비인접 좌표에만 쓰며 Facility 드레싱도
    3/32로 제한한다. 첫 던전은 flat 3종만 사용하고 `-raised`는 후속 던전용으로 보존한다.
    잔여 배치 ① 범위는 hole/weak-floor/ladder(규격 결정 포함)다.
  ③ 아케이드 소품(위 "아케이드 소품 추가"와 합류 — 구 병원 소품 발주는 집행 전 폐기) + 낙하 연출 ④ 아이템 12종 리스킨. 소스시트가 리포에 들어오면
  `process_postapoc_*` 재실행으로 마감한다. **코드 선행분은 이미 완료**돼 있다(아카이브).
  - 2026-08-03 작업 트리에서 일반 적 5종과 감시자의 남향 정적 폴백을 전용 아케이드 점유 세력
    실루엣으로 교체해 구 약탈자·판타지·절차 폴백 노출을 닫았다. 당시 남았던 정식 방향 애니메이션과
    PC 실화면 배율 승인은 아래 2026-08-04 항목에서 완료했다.
  - [ ] **적 액터 치비 레인 — 1라운드 채택 없음(2026-07-31)**: 레시피 3종
    (`actor-{slinger,grave-warden,arc-drone}-chibi-base-v1`)을 신설하고 감시자를 생성했으나,
    배경이 액자 패널로 구워지고 가슴 코어가 플레이어 정체색(마젠타)과 겹쳐 **기각·되돌림**.
    비율·접지는 계약을 지켰다 — 고칠 것은 프롬프트 2줄이다. 소견·다음 라운드 지시는
    `docs/art-direction/enemy-chibi-lane-round1-findings.md`.
  - [x] **배치 ② vertical slice — 메인 원정자 정적 접지**: 초기 6태그 자동 조립본은 프레임
    일관성 미달 이력으로 남기고, 승인 컨셉에서 다시 만든 `96×128` 단일 `Frame_0`을
    `actor-knight.aseprite`에 연결했다. 하드 알파·24색 역할 팔레트·2×2 클러스터와 발 기준선을
    잠갔고 local light·접촉 AO·열린 코너 마커까지 PC 화면에서 확인했다. 이 정적 기준은 아래
    방향 타임라인의 태그 밖 첫 프레임으로 그대로 보존한다.
  - [x] **4방향 전투 프레젠테이션 브리지(2026-08-03 작업 트리)**: 월드 facing과 시점 회전 뒤
    화면 facing을 분리하고 `상태-방향` Aseprite 태그 우선/무방향 폴백 계약을 연결했다. 승인 프레임이
    없던 브리지 단계의 액터도 정적 4방향 포즈, 방향성 근접 궤적·원거리 총구 섬광/반동·피격 반동/버스트와
    종류별 투사체를 최소 표시 프레임으로 재생한다. PC 근거는
    `directional-{melee,ranged,hit}-pc-2026-08-03.png`다.
  - [x] **메인 원정자 4방향 6상태 타임라인(2026-08-03 작업 트리)**: 승인 `Frame_0`을 태그 밖
    첫 프레임으로 보존하고 `idle/walk/attack/hit/fall/death × north/east/south/west` 24태그,
    태그 프레임 80개를 `actor-knight.aseprite`에 승격했다. 루프 끝에 Unity 임포터가 붙이는 마지막
    유지 키는 런타임 베이크에서 제외해 실제 authored 프레임 수와 재생 박자를 맞췄다.
    `SurvivorAnimationApproved=true`로 게이트를 열었고, 이동·공격·피격 키포즈와 방향성 전투 FX를
    PC Game View에서 함께 승인했다. 제작 전수 미리보기는
    `actor-knight-directional-conform-preview-v1.png`, 실화면은
    `actor-directional-{walk,attack,hit,fall}-runtime-pc-2026-08-03.png`다. `fall`은 0.21초
    클립이 끝나도 0.48초 월드 낙하가 착지할 때까지 마지막 프레임을 유지한다. 적 6종도 아래 항목에서
    같은 방향·상태 계약으로 승격했다.
  - [x] **배치 ② 적군 정체성 정적 로스터 1차(2026-08-03 작업 트리)**: 코드 ID·전투 스탯·혼합표는 유지한 채
    일반 적 5종과 감시자를 점거군·기업 병력·산업 로봇·사이버사이코 실루엣으로 모두 교체했다.
    built-in ImageGen 소스를 `process_arcade_occupation_actors_v1.py`가 경계색 크로마 제거 후
    96×128·하드 알파·공용 팔레트·2×2 클러스터·발 기준선으로 마감하고 여섯 카탈로그 슬롯에 연결한다.
    인간형 감시자의 `CanClimb`만 실루엣 계약에 맞췄고 현 보스 아레나 동선에는 영향이 없다.
    당시 정적 승인본은 아래 1차 방향 원본의 태그 밖 `Frame_0`과 `idle-south[0]`으로 보존했다.
  - [x] **적군 사이버펑크 테마 회귀 차단(2026-08-04 작업 트리)**: 카탈로그 누락 시의 절차 폴백
    6종을 점거군·기업 병력·산업 로봇·사이버사이코로 다시 그렸고, unknown ID도 녹색 Goblin 대신
    중립 보안 드론으로 분리했다. `generate_runtime_art_v2.py`와 구
    `process_postapoc_actors_v2.py`가 승인 적 PNG를 판타지 구판으로 덮어쓰지 못하게 적 출력 소유권을
    `process_arcade_occupation_actors_v1.py` 하나로 고정했다. 실제 카탈로그 asset의 6개 정식
    `Frame_0`·방향 24클립 바인딩, 6개 표시명과 금지 토큰, 텔레메트리 UI 현지화를 회귀 테스트한다.
  - [x] **배치 ② 적군 4방향 6상태 타임라인 + 전투 FX 1차 승인(2026-08-04 작업 트리)**:
    `actor-{goblin,skeleton,slime,slinger,arc-drone,grave-warden}.aseprite` 여섯 원본을 각각
    `Frame_0 + 태그 프레임 80개`, 24태그(`idle/walk/attack/hit/fall/death × 4방향`)로 승격했다.
    east/west는 전 상태 화면 기준 exact mirror이고 north는 별도 후면 재질·실루엣을 쓰며, 인간 돌격·
    견착 사격·피스톤 보행·궤도 주행·드론 활주·중장 외골격의 동작군을 분리했다. 살아 있는 프레임에는
    단 하나의 적색 IFF run을 유지하고 사망 마지막 프레임에서 소등한다. Unity 카탈로그는 원정자까지 7개 방향 세트를
    베이크한다. walk 한 주기는 실제 한 칸 이동 시간에 정규화해 세 키포즈를 모두 노출하고, 적 낙하는
    보이는 출발/도착이면 0.22초 실제 하강 보간 뒤 `hit`에 덮이지 않은 채 `fall` 마지막 프레임을 유지한다. PC QHD에서
    이동·공격+방향성 근접/원거리 효과·피격+종류별 충격 버스트·낙하/사망 홀드를 확인했다. 근거는
    `docs/captures/enemy-directional-{walk,attack-fx,hit-fx,fall-death}-runtime-pc-v1.png`, 전수 시트와
    동작 미리보기는 `arcade-enemy-directional-{conform-preview-v1.png,motion-preview-v1.gif}`다. Unity 2D
    Aseprite Importer 5.0.x가 같은 캔버스·atlas 크기의 재임포트에서 이전 Sprite rect/UV를
    재사용해 액터가 조각나던 경우는 `actor-*` 임포트 직전 이전 atlas 크기만 무효화해 GUID를
    보존한 채 다시 pack하도록 막았다. 대표 방향 프레임의 imported rect가 실제 알파 네 경계에
    모두 닿는지도 EditMode 회귀가 검사한다. 이 승인과 캡처는 아래 2차 정체성·팔레트 재작업 전
    로스터의 기록이며, 새 자산의 승인 근거로 재사용하지 않는다.
  - [x] **적군 정체성·팔레트 2차 교정·PC 승인(2026-08-04 작업 트리)**: 호환 코드 ID `Slime`의
    표시명과 얼굴을 **기업 추적 드론**으로 교체했다. 약하고 흔하게 넓게 배회하는 기존 역할과 중독 2턴,
    `CanClimb=false`는 유지하고, 낮은 사족 보안 드론에 쐐기형 센서 헤드·노출 서보 척추·손상된
    군중제압제 카트리지의 제압제 주입턱을 부여했다. 적 6종의 재료색도 차콜·건메탈 지배로 옮기고
    녹·갈색은 마모에만 제한했으며, 시안/마젠타는 국소 기술 악센트, 적색 IFF는 작은 한 점으로 고정했다.
    Core shim 1203/1203, ArtPipeline 312/312, Telemetry 18/18, Unity EditMode 1496/1496, PlayMode 24/24를
    통과했다. Unity는 69개 Aseprite 원본·69개 슬롯·7개 애니메이션 세트를 재검증했고, PC Game View의
    여섯 런타임 카탈로그 프레임은 `enemy-cyberpunk-runtime-catalog-pc-2026-08-04.png`에 보관했다.
  - [x] **배치 ④ fallback vertical slice — 아이템 12종**: 아이템별 단일 소스를 생성한 뒤
    `process_items_v3.py`로 64×64/하드 알파/공용 팔레트/바닥 피벗을 잠갔다. 액션 UI도
    32px 소스/24px 표시로 올렸고(현재 PC Field Deck은 별도 12px glyph, Mobile은 이 세트 유지), HUD·인벤토리 검수본은
    `item-ui-integrated-{hud,inventory}-v3.png`에 보관했다. 정식 Aseprite 승격은 교체 가능한
    후속 단계로 남지만 조악한 절차 아이콘은 런타임에서 교체됐다.
  - [x] **배치 ④ 세계관 정체성 정리(2026-08-03 작업 트리)**: 1차 ComfyUI 배치는 12종 중 11종을
    교체했지만 자연물 버섯과 마법 얼음검처럼 읽히는 두 슬롯이 남았다. 저장 호환을 위해 enum 숫자와
    `item-herb`/`item-frost-shard` 경로는 그대로 두고, 화면 정체성·표시명만 **지혈 패치/PATCH**와
    **냉각 코일/COIL**로 바꿨다. 현재 provenance는 ComfyUI 10종 + built-in ImageGen 2종이며,
    64×64/하드 알파/팔레트/피벗 계약은 불변이다. 과거 1차 비교본은
    `docs/captures/item-arcade-reskin-v1.png`에 남긴다.
- [x] **UI 레이아웃·시인성 재설계** (5 Phase, `2e7cf5f`→`977eba4`) — 시인성 문제의 실체는
  도트 선명도가 아니라 **상대 크기**였다(정수 배율·`fontRenderingMode` 가설 둘은 실측으로 기각,
  `docs/captures/spike-*.png`). 논리 캔버스를 960×540 → **640×360**으로 옮겨 같은 논리 px가
  화면에서 약 50% 커지게 하고, `PanelSettings`를 `ConstantPixelSize`로 돌려 **배율 소유권을
  에셋에서 코드로**(`Core/UiPanelScale`) 가져왔다. PC 3종(1280×720·1920×1080·2560×1440)이
  전부 정확히 같은 캔버스로 떨어져 해상도 분기가 사라졌다. 최소 지원 창은 1280×720.
  - Phase 2 던전 HUD: 앵커 6개 → **4구역 + 1과도 밴드**. `name=`은 하나도 바꾸지 않고 역할만
    재배정했다. 새 읽을거리 셋(상태이상 칩·메시지 로그·층 스택)은 전부 **이미 있던 데이터**를
    화면에 꺼낸 것이다 — 기둥 ②가 처음으로 화면에 나왔다.
  - Phase 3 인벤토리: "소지품"이 잘리던 원인은 배율이 아니라 336px 하드코딩 대 320px 페인이었다.
  - Phase 4 허브·메인 메뉴: 타이포·크롬 정렬 + `is-locked`→`locked` 통일.
  - Phase 5: 화면공간 비네트(URP 포스트프로세싱 아님) + 등잔 아트↔빛 커플링 버그 수정.
  - 크롬 어휘를 여섯에서 **둘**(플레이트/창)로 줄였다. 이건 `docs/UI_DESIGN_SYSTEM.md`의
    "인게임에 창 금지" 규칙을 사용자가 완화한 결과다 — 몰입보다 시인성이 앞선다.
  - **후속**: 허브 화면은 Phase 4에서 타이포만 옮기고 **치수를 빠뜨렸다**(아이콘 버튼 44×44,
    `.ui-icon` 24px, 출정 준비 카드 960px). 별도 커밋으로 640×360에 맞췄고, 같은 작업에서
    Phase 3이 남긴 `.settings-reset` 폭 붕괴 회귀를 찾아 고쳤다.
  - **남은 부채**: ① 모바일은 재설계하지 않았다(깨지지 않을 정도만 유지 — CLAUDE.md 우선순위).
    390×844에서 루트 밖으로 나가는 요소 0개를 확인했다(`ui-redesign-mobile-390x844.png`).
    ② 내구도 시스템은 도입하지 않았다 — 부채가 아니라 결정이고, 판단 근거는 `docs/SYSTEMS.md` §백팩.
  - **DesignSystem 기본값 이전 완료**: 컴포넌트 기본 크기도 640×360 기준으로 옮겼다
    (`.pc-dia` 34→24 · `.pc-dbtn` 62→40 · `.pc-slot` 60→40 · `.ui-icon` 24→12 · `.pc-heart` 24×21→12×11 …).
    공유 기본값은 **라이브 화면이 실제로 쓰는 값**에 맞췄고, 라이브 렌더가 안 바뀌는 것을
    요소별 실측으로 확인했다(유일한 변화는 의도한 `.pc-btn` 보더 2→1). 갤러리는 ScrollView 로
    감싸 640×360 에서도 컴포넌트 전부를 볼 수 있게 했다.
  - 캡처: `docs/captures/ui-redesign-*.png`
- [x] **임시 통합 아트 패스** — 환경→UI→액터 단계 게이트로 기존 포스트아포 소스시트를
  공용 팔레트에 재잠그고, 메인 메뉴 폐병원 배경·UI 아이콘/9-slice·카탈로그 액터/소품을
  PC Game View에 반영했다. 던전 기준 카메라는 4.0→3.2로 좁혀 캐릭터와 재질을 읽히게 했고,
  메인/허브/던전 최종 캡처를 `docs/captures/integrated-art-pass-*.png`에 보관했다.
  - [x] **폐병원 hero-room 품질 패스** — 바닥 3종·벽 3종(방향 포함 9개 슬롯)을 추가하고
    카메라를 2.3, 액터 그림 배율을 0.72로 조정했다. 얕은 층 앰비언트와 플레이어/벽 등잔
    대비도 다시 잡아 `hospital-hero-room-art-quality-v1.png`에서 화면 점유율·재질 반복·광원 구역을
    함께 검수했다. 들것·수술등처럼 별도 충돌/배치가 필요한 소품은 위 미완 항목에 남긴다.
  - [x] **허브/던전 화면 배율 통일** — 공용 해상도·PanelSettings는 유지하고, 13×9 허브를 전부
    담느라 카메라가 물러나던 auto-fit을 제거했다. 양쪽 모두 `playCameraSize` 2.3으로 플레이어를
    추종하며 던전 DebugAll만 별도 조감 배율을 쓴다. 동일 1280×720 비교 캡처는
    `lobby-game-scale-{hub,dungeon}-1280x720.png`에 보관했다.
- [x] **PC 현재 층 자유 카메라(2026-08-03 작업 트리)** — 던전 PLAY에서 중클릭 드래그로 현재 활성 층의
  mapped 투영 경계 안에서 카메라 중심만 옮기고, 릴리스 뒤 위치 유지와 `Home`/`Escape`/미니맵 플레이어
  마커 클릭 및 행동·시점 전환
  추종 복귀를 연결했다. 배율·턴·FOV·AI·미니맵은 불변이며 드래그와 월드 탭을 분리했다.
  Core shim·Unity EditMode/PlayMode 전체 회귀와 컴파일 오류 0건을 확인했고, 전후 PC Game View는
  `camera-look-{follow,panned}-pc-2026-08-03.png`와 실제 마커 클릭 전후
  `camera-minimap-recenter-{panned,follow}-pc-2026-08-03.png`에 보관했다.
- [x] **PC Q/E 배율 고정·수직 관찰 판독성(2026-08-04 작업 트리)** — 일반 PLAY는 B2 hero-room까지
  `playCameraSize` 2.3으로 통일해 Q/E와 문 진입·이탈에서 크기가 바뀌지 않게 했고, 명시적 수직 관찰만
  네 시점의 필요 배율 중 최댓값을 공용으로 쓴다. 정적 액터 폴백의 방향별 축소도 없앴다.
  명시적 위/아래층 관찰은 시안 컷어웨이·마젠타 선택 버튼·`관찰 전용/이동 불가` 상시 문구와
  읽기 전용 X/`VIEW ONLY` 클릭 응답을 얻었다. active floor·턴·플레이어 위치는 불변이며 층간 광역 투척은
  기존 유효 칸 마커가 우선한다. Core shim·Unity EditMode/PlayMode 전체 회귀와 실제 PC q0..q3 동일 배율,
  `vertical-observe-{down,up}-pc-2026-08-04.png`, 문 경계
  `camera-door-{inside,outside}-pc-2026-08-04.png`를 확인했다.
- [x] **적 애니메이션 재생기 배선** — 스폰 시 `SpriteClipAnimator`를 붙이고 카탈로그 클립을
  넘긴다. Unity `Animator`가 `null`인 것은 의도된 구조다. `idle-north` 같은 방향 태그도 현재
  facing으로 선택하고 시점 회전 때 같은 상태의 방향 클립으로 교체한다. 배치 ② 전용 적 6종 원본도
  1차 2026-08-04 작업 트리에서 실제 24개 방향 태그 클립으로 연결됐다. 2차 정체성·팔레트 재작업 뒤에도
  Aseprite 재동기화와 전체 EditMode 회귀에서 동일 바인딩·고정 GUID가 유지됨을 확인했다.
- [x] **SPD식 전체 mapped 목적지 이동(2026-08-04 작업 트리)** — FOV와 분리된 현재 층 공용 실루엣을
  클릭 가능한 지도 지식으로 만들고, 일반 닫힌 문을 열기 1턴/FOV/적 턴/인터럽트/재계획 순서로 통과시켰다.
  비밀방·미확인 특수 정보는 숨기고 층 전환 계단/사다리 링크의 자동 관통을 막았다. 공용 범주 스냅샷으로
  시야 밖 지형 변화 누설도 차단했다. 후속 판독성 패스는 실제 타일 면을 재사용하던 mapped 표현을 중립
  점묘·점선·범주 기호로 교체해, 실제 재질·연속 아이스 면을 쓰는 다른 층 관찰 조각과 형태로 분리했다.
  Core·PlayMode 회귀와 PC 외형 근거는 각각 관련 테스트 및
  `docs/captures/layer-grammar-{current,vertical-compare}-pc-2026-08-04.png`가 소유한다.
- [x] **위협 이동 예산 통일·하단 상태 피드백(2026-08-04 작업 트리)** — 시야에 살아 있는 적이 있으면
  모든 자동 접근을 `이동 1칸` 또는 `인접 행동 1회`로 제한하고, 접근 뒤 공격·문·구출·휴식·탈출·프롭
  행동이 같은 입력에서 연달아 나가던 예외를 없앴다. 마지막 접근 칸에서 새 적·피해·아이템을 만나도
  후속 행동 전에 멈춘다. PC 하단 기존 턴 칩은 `자동 이동`/`위협 · 1행동`을 표시하며 수직 관찰의
  `관찰`이 우선한다. Mobile의 기존 `내 턴` 배치는 유지한다. PC 실화면은
  `docs/captures/travel-mode-{safe,threat}-pc-2026-08-04.png`로 승인했고, Core shim 1222/1222,
  Unity EditMode 1528/1528, PlayMode 30/30과 최종 컴파일 오류 0건을 확인했다. PlayMode는 실제
  위협 접근 1칸·적 페이즈 종료 HUD 갱신·인접 구출 1턴 소비까지 고정한다.

### Unity 화면 확인 대기

> 파일 반영 뒤 **Unity 화면 확인 또는 현재 작업 트리 재검증이 남은** 것들이다. 에디터가 붙은 세션에서만 가능한
> 항목은 여기서 닫는다.
> 2026-07-26 `59c5f80` 에디터 세션에서 대부분 처리했다 — 실측치는 `docs/STATUS.md`가 진다.

- [x] 드론이 캐치워크를 못 따라오는지(사다리 추격 단절) · 개구부 너머 닫힌 문 뒤가 실제로 안 보이는지.
  둘 다 **살아 있는 던전에서 확정**했다(합성 테스트 픽스처가 아니라 실제 생성 결과로).
- [x] 합선 검사 드론의 스폰·애니메이션 슬롯과 전용 96×128 정적 폴백. 2026-07 검수에서 찾은
  작은 절차 폴백은 2026-08-03 전용 자산으로 교체했다. 정적 PNG에는 `Animator`를 붙이지 않는 것이
  현재 의도이며, 승인 태그 클립이 생기면 공용 `SpriteClipAnimator` 경로가 자동으로 사용한다.
- [x] 기업 추적 드론 교체와 적 6종 냉색 팔레트의 현재 PC Game View 승인.
  `enemy-cyberpunk-runtime-catalog-pc-2026-08-04.png`에서 실제 카탈로그 여섯 프레임을 한 화면으로 확인했다.
- [x] **첫 던전 단일 이동 평면화** — 한 층의 이동 가능 타일을 기준 elevation 하나로 통일하고,
  층내 계단·사다리·캐치워크 생성을 끈 실제 화면을 `first-dungeon-flat-v1.png`로 확인했다.
  층간 계단·Hole·낙하·엘리베이터와 elevation stride는 유지한다.
- [ ] **mapped 장거리 이동 동적 UX 승인** — 실제 PC 입력으로 원거리 Unknown 클릭 → 목적지 표식 →
  문 열기 1턴 → 재계획 → 도착을 보고, 적이 이미 보일 때 탭당 한 행동·새 위협 인터럽트·재탭 취소가
  행동 경계에서 일관된 문구와 함께 멈추는지 한 흐름으로 확인한다.
- [ ] 첫 던전 벽 등잔 밀도(깊이별 기울기) — 치트 층 점프로는 판단이 어려웠다. 캐치워크 격자 룩은
  첫 던전 범위에서 빠졌으며, 층내 높이를 쓰는 후속 던전의 화면 검증 항목으로 옮긴다.
- [x] 보스 접근 전조의 **화면 노출 타이밍(2026-08-04 PC 확인)** — 6F에서 실제 상행 계단을 밟아
  7F(진행지수 8)에 진입할 때 `천장 너머가 낮게 울린다 — 한 층 위에서`가 정확히 한 번 노출됐다.
  같은 층 이동·반복 갱신에서는 재표시되지 않고 보스 처치 상태에서는 침묵한다. 전환 전/도착 직후
  근거는 `boss-approach-{before-6f,arrival-7f}-pc-2026-08-04.png`, 화면 렌더 없는 동일 경로 재실행은
  Unity 콘솔 오류 0건이었다.

## 현재 개발 우선순위 (임시)

- **모바일 보류 · PC 우선.** 모바일 세로·다해상도 검증과 이원 View 유지가 개발을 느리게 해서,
  당분간 PC(데스크톱)만 개발·검증 타깃으로 둔다. 상세 규칙은 `CLAUDE.md`의 "현재 개발 우선순위".
- 입력 추상화·`ResponsiveUiLayout`·이원 UXML 코드는 **삭제하지 않고 유지**한다(재개 시 재작업 방지).
  PC 에디터/빌드는 `HudPresentation.Auto`가 이미 `Desktop`으로 해석되므로 별도 코드 변경은 없다.

## 환경 현황 (2026-07-26)

- Unity **6000.5.4f1** (`ProjectSettings/ProjectVersion.txt`가 버전 SSOT).
- **Unity MCP 연결됨** — `run_tests`/`manage_scene`/`read_console` 사용 가능. 단 웹·원격 세션에는
  에디터가 없어 Core shim 만 돌아간다.
- **DOTween Pro** 설치됨(`Assets/Plugins/Demigiant/`) + 부트스트랩 `Gameplay/DOTweenBootstrap.cs`.
  **호출부는 아직 0곳** — 아래 「리팩토링」의 결정 대상.
- asmdef 5개: `ProjectC.Core`, `ProjectC.Gameplay`, `ProjectC.ArtPipeline.Editor`,
  `ProjectC.Tests.EditMode`, `ProjectC.Tests.PlayMode`.

## 리팩토링

> 기준은 크기가 아니라 **경계**다. 같은 규칙이 여러 곳에 손으로 복사돼 있으면 한 곳만 고쳐도
> 조용히 갈라지므로, 공용 이터레이터·헬퍼로 모으는 것을 먼저 한다.

이전 완료분(아이템 저장 구조 평탄화 · 아이템 분류 일원화 · `IsoPrototypeDemo` 1단계)은 아카이브에.

- [x] **블라스트·젖은 웅덩이 순회를 공용 이터레이터로** — 3×3 블라스트 이중 루프와 젖은 웅덩이
  4방향 확산이 여러 파일에 손으로 복사돼 있었다. 순회 순서가 곧 결과 순서라 한 곳만 어긋나도
  골든 지문이 조용히 갈린다. `BombRules.ForEachBlastCell` · `WetPoolFlood.Collect`
  (`Core/WaterRules.cs`) 둘로 모았고, 순서 불변이라 골든 지문이 그대로 통과하는 것이 증명이다.
- [x] **생성기의 하드코딩 방 기하를 `FloorPlan`으로** — 입구 칸·북쪽 방/분기 방 rect가 생성기
  곳곳에 상수로 박혀 있어 방 배치를 바꾸면 하나를 빠뜨리게 돼 있었다. `UpperRoomEntrance`/
  `IsUpperRoomEntrance`/`UpperRoomCells()`/`BranchCells()`로 모으고, `PlaceRestSite`를
  `Carving` → `Placement`로 옮겨 파셜 경계를 굳혔다: **타일을 바꾸면 Carving, 좌표만 고르면 Placement.**
- [x] **죽은 코드 정리** — `ElevatorShaftRules.IsEntranceFloor`·`FallResult.Landing`은 소비자가 0이라
  지웠다. 반면 `FallRules.DamageForFloors`는 **남긴다** — 프로덕션 미사용이지만 층 기반 기준 곡선을
  재현하는지 고정하는 테스트 오라클이라, 지우면 곡선이 조용히 바뀐다.
- [x] **원거리 AI 사격 위치 탐색에 등반 정책 전달** — 일반 추격은 종별
  `MonsterArchetype.CanClimb`을 넘겼지만 `CombatRules.FindFiringPosition`은
  `GridPathfinder` 기본값(true)을 써서 합선 검사 드론도 사다리 위 사격 자리를 계획할 수 있었다.
  플레이어는 기존 기본값을 유지하고 몬스터만 아키타입 값을 명시해, 인간형 사수와 기계 드론의
  대조를 Core 회귀로 고정했다.
- [x] **인접 상호작용 접근 경로가 적을 우회** — 적을 무시한 최단 경로를 만든 뒤 폐기하던 구조는
  한 칸 긴 안전한 길이 있어도 상호작용을 실패시켰다. `InteractionApproachRules`가 점유자를
  A* 차단 조건으로 주입하고 결정론적 인접 후보 선택을 Core 회귀로 고정했다. 경로 끝은 대상
  자체가 아니라 같은 높이의 인접 칸이라는 계약도 공유해, 중간 탈출구가 항상 실패하거나
  이동 중단 뒤 NPC가 원격 구출되는 소비자별 불일치를 막았다.
- [x] **텔레메트리 층 라벨 동결 + 방향 중립 최종 구역 문구** — v6부터 최초 표시 라벨을 저장해
  과거 리포트를 당시 값으로 보존한다. 구 리포트는 현재 카탈로그로 한 번 복원한 뒤 라벨을
  물질화해 다시 바뀌지 않게 한다(v1~v4는 역사적 `-floorIndex`로 진행 지수를 먼저 복원).
  새 기록은 스테이지 누적값으로 라벨을 재계산하지 않고 실제 화면 라벨을 넘긴다. 최종·중간
  출구 안내의 하강 전용 `최심*`/`더 내려가면` 및 고정 `▼` 화살표도 제거했다.
- [x] **허브 UI 콜백 수명주기 대칭화** — 버튼·포인터 delegate를 레지스트리에 보관해
  `OnDisable`/재바인딩 전에 해제한다. 허브 재활성화 횟수만큼 구매·이동·씬 전환이 중복되던
  경로를 EditMode 회귀로 막았다.
- [x] **원거리 충전 저장 계약 고정** — 체크포인트·던전 전환은 상태 스냅샷을 쓰고 남은 충전과
  회복 턴을 함께 보존한다. 세이브 v2 마이그레이션이 `JsonUtility`의 누락 중첩 필드=0/0 동작을
  원문 필드 존재 여부로 구분해 구세이브만 만충으로 복원하고, 실제 저장된 0/0 방전 상태는
  보존한다. 이스케이프된 합법 키도 디코딩하고, 미래 런/텔레메트리/메타 스키마는 이어하기·
  재저장을 차단해 알 수 없는 필드를 원문 그대로 보존한다. 미래 메타가 있으면 허브 변경과 출정을
  막고, 런 도중 감지돼도 정산 성공 전 인벤토리·체크포인트를 유지한다. Core·JSON·PlayMode 회귀를
  모두 둔다.
- [x] **런 종료 정산 멱등화** — 메타 저장 성공과 체크포인트 삭제 사이에 앱이 종료되면
  같은 전리품·장비·의뢰·기록·해금을 다시 지급할 수 있었다. 세이브 v3의
  `RunSettlementEntry`가 종료 효과 전부와 같은 원자 저장에 들어가고, 재개 시 `runId`
  영수증을 먼저 조회한다. 메타 저장 뒤 체크포인트가 남은 상태를 직접 만든 PlayMode 회귀에서
  장비 반환·전리품·완료 의뢰·기록이 두 번째 생환에도 늘지 않음을 고정했다.
- [ ] **`IsoPrototypeDemo` 2단계 — 실제 분리**(월드 빌드 / 턴 실행 / 연출). 1단계와 달리 이쪽은
  **상태를 진짜 공유**해서 경계를 새로 설계해야 한다 — 파셜 쪼개기로는 안 된다.
  - [x] 첫 슬라이스: `HubWorldRegistry`가 상호작용 id/라벨과 프롭·광원 앵커를 소유한다.
    `BuildPrototype` 공용 초기화에서 참조를 비우고, `VisualPosition` 주입 + `IsoGrid` 정렬로
    시점 회전 계약을 보존했다. 단위 계약과 실제 허브 회전 PlayMode 스모크를 함께 둔다.
  - [x] 둘째 슬라이스: `HubWorldPresenter`가 시설 개방 불변 스냅샷을 입력받아 프롭·광원 생성과
    상호작용/앵커 등록을 소유한다. 호스트는 `MetaStore`를 한 번 읽어 스냅샷으로 바꾸고 씬 의존성만
    주입한다. 시설 4조합·광원 필터·카탈로그 폴백·환경 애니메이션과 동일 인스턴스 재빌드를 검증한다.
  - [x] 남은 슬라이스: 낙하/폭발 시퀀스 서비스 **(2026-08-01 완료)**.
    - [x] **상태 DTO와 회귀 테스트**: `Core/HazardSequence`가 연쇄의 **순서**를
      가져갔다 — 피해→상태→원소 반응→넉백→유폭을 `HazardStep` 목록으로 남기고, 규칙 자체는
      기존 `BombRules`/`KnockbackRules`/`FallRules`/`OilRules`/`WaterRules` 그대로다.
      코루틴에만 있던 판정(유폭 조건·플레이어 사망 중단·붕괴→낙하 순서·안전 낙하 높이가
      플레이어 전용인 것)을 EditMode 15개로 고정했다. 근거는 `ARCHITECTURE.md` §8.3.
    - [x] **씬 와이어링**: `IsoPrototypeDemo.Falls`가 스텝을 재생만 한다 — 진입점
      (`FallPlayer`·`CollapseUnder*`·`ResolveExplosion`·`KnockbackCombatant`) 시그니처는 그대로 두고
      본문에서 판정을 전부 걷어냈다. 스텝 열거를 **지연**시켜 "폭발 애니메이션 → 피해 → 밀려남"의
      끼어드는 순서를 보존한다(한꺼번에 판정하면 첫 연출에서 이미 적이 다른 층에 있다).
      부여와 연출이 붙어 있던 `ApplyStatusWithPresentation`은 `PresentStatusApplied`로 갈라
      Core가 부여한 결과를 화면에만 반영한다. Core 1095 · EditMode 1306 · PlayMode 10 통과.
      **화면 검수는 아직**이다 — 자동 회귀는 연출 문구의 순서까지는 보지 않는다.
- [ ] **공용 Tween 추출** — 코루틴 보간이 여러 곳에 흩어져 있다. 다만 CombatFx 쪽은 루프가
  프레임별 생존 확인을 품고 있어 단순 시그니처로는 못 옮긴다.
- [ ] **DOTween 사용 여부 결정** — 설치·asmdef 참조·부트스트랩까지 있는데 호출부가 0곳이다.
  쓸 것이면 위 Tween 추출이 DOTween 위에 서야 하고, 안 쓸 것이면 플러그인과 예제를 정리한다.
  **이 결정이 Tween 추출보다 먼저**다.
  - **결정 보류(2026-08-01)**: 지금 넣지 않는다. 실제 연출 작업을 할 때 그 자리에서 정한다.
  - 그때 다시 재지 않도록 이번 실측을 남긴다: 플러그인 2.9MB·23파일, `DOTweenBootstrap`이
    실행마다 `DOTween.Init` + 트위너 500·시퀀스 100을 사전 할당하는데 **호출부는 0곳**이다.
    옮길 대상은 코루틴 보간 17곳(Movement 6 · CombatFx 5 · Actions 2 · Enemies 2 · Falls 1 ·
    FloatingTextSpawner 1)인데, **한 프레임에 위치·정렬·스케일·알파를 같이 굴리는 복합 루프**라
    `DOTween.To(...).OnUpdate(...)` 안에 지금 코드가 그대로 들어간다 — 줄지 않는다. 특히
    `ApplyMovingActorVisualSorting`은 위치와 같은 프레임에 갱신돼야 해서 분리되지 않는다.
    체이닝·이징이라는 DOTween의 이점은 지속시간 0.08~0.3초·기본 Linear인 이 게임에서 쓸 데가 적고,
    턴 루프가 완료를 기다려야 해 `WaitForCompletion()`을 한 겹 더 감싸야 한다.
  - 따라서 **「공용 Tween 추출」을 DOTween 위에 세울 이유는 지금 없다** — 필요해지면 코루틴
    헬퍼로 먼저 모으고, DOTween은 그걸 대체할 만한 연출(UI 시퀀스 등)이 생길 때 판단한다.
- [x] **입력 헬퍼 통합(2026-08-03)** — HUD의 `Escape`·`I`·`F1`·Cmd/Ctrl+D 단발 판정을
  `HudKeyboardInput` 한 곳에 모으고 네 컨트롤러의 Input System/legacy 분기를 제거했다.
  모달 닫기 우선순위는 소비자에 그대로 두고 키 매핑·modifier·hold 비반복은 PlayMode로 고정했다.
  월드 이동·Tab 액션 휠·중클릭 팬·포인터/터치는 `IsoTapInput` 소유라 이번 범위에서 바꾸지 않았다.

## 백로그 — 관찰 & 정리 제안

> `docs/ARCHITECTURE.md` §14에서 넘어왔다. 리스크가 낮은 **검토 대상**이지 즉시 실행 항목이
> 아니다 — 아키텍처 문서는 "코드가 지금 어떻게 배치돼 있나"만 지고 백로그는 여기가 진다.

- [ ] **`DungeonFloorInfo.EnemySpawn`** 단일-적 호환 shim(`EnemySpawns[0]`)은 다중 스폰 전환의
  잔재다. 사용처를 정리한 뒤 제거를 검토한다.
- [ ] **크로스-층 몬스터 활성화** — `MonsterActivation`이 지금은 같은 층으로 의도적으로 좁혀져
  있고, "보이는 구멍 인접의 아래층 몬스터" 같은 층을 넘는 규칙 자리가 주석으로 예약돼 있다.
- [ ] **문서의 변동 수치 최소화** — Unity 버전은 `ProjectSettings/ProjectVersion.txt`, 파일/파셜
  지도는 `docs/CODE_STRUCTURE.md`, 현재 동작은 `docs/STATUS.md`가 SSOT다. 복사본은 같은 속도로
  낡지 않는다 — **이 로드맵은 실측치(줄 수·파일 수·테스트 개수)를 들지 않는다.**

## 향후 기술 과제 — 던전 생성기 BSP/룸-앤-코리더 전환

> 현재 생성기(M1)는 **3방 고정 템플릿 + seed 변형**이다. 이는 다층 던전의 **생성 불변식을 먼저
> 못박기 위한 발판**이며, 최종 목표는 `GDD.md` §5.8의 진짜 룸-앤-코리더/BSP 생성기다
> (근거: `DungeonLayout.cs` 상단 주석, `SYSTEMS.md`의 연결 그래프·불변식 유지 문장).
> 교체 대상은 생성 알고리즘뿐 — 불변식은 계약으로 유지한다.

- [ ] **생성기 내부를 BSP/룸-앤-코리더로 교체** — 방 배치·복도 카빙을 알고리즘으로 다양화한다.
- [ ] **불변식 계약 테스트를 생성기 구현과 분리**(제너레이터 무관) — BSP로 갈아끼워도 그대로 통과해야 한다:
  문 밀봉(열면 도달·닫으면 북쪽 방 차단) · Hole은 정확히 한 층 아래 walkable 착지(2층 관통 금지) ·
  층 간 기둥 겹침으로 착지 컬럼 보존 · 적은 문 뒤에만 · 진출 계단 경비 · 분기/비밀 방 보상.
- [ ] **층-간 정렬 설계** — 각 층을 독립 BSP로 돌리면 "구멍이 아래층 진짜 바닥에 착지"가 공짜로 안 나온다.
  낙하·층간 시야·비밀방·AI가 이 정렬에 의존하므로, BSP 분할 위에서 정렬을 보장하는 방식이 전환의 핵심 난도.
- 상세 설계 배경: `docs/ARCHITECTURE.md` §7.4.

## 향후 기술 과제 — 3D 시야선(높이 인식) · 입체 전투

> 시야/사격 판정은 원래 같은 높이만 알았고 세 곳에 흩어져 있었다. **세 축 모두 `SightRules`
> 하나로 합쳐졌다**(3단계 완료, 경위는 아카이브) — "높이가 달라도 실제로 보이면 보고 쏜다"가
> 성립한다. 첫 던전은 **한 층=한 이동 평면**을 기준으로 삼고, 이 토대는 층간 Hole·낙하와
> 층내 높이를 허용하는 후속 던전/특수 세트피스에 남긴다.

지켜야 할 뿌리(불변):
- **void = 불투명** ("닫힌 문 뒤 방 = Unknown") — void 컬럼은 무한 높이 벽으로 취급해 유지.
- **렌더 ≠ 시뮬** — 시야가 높이를 알아도 화면은 여전히 활성 층 + Hole만 노출.
- **컬럼당 여러 솔리드 구간(천장/공중)** 인식 — 위층 바닥이 아래층 위에 얹히므로 높이맵이 아니라 span.

관련 향후 과제(방향만, 미확정):
- [ ] **후속 던전용 입체 전투**: 근접 단차 타격·내려치기 보너스, 고지대 원거리, 근접 넉백으로
  발판 아래 낙하 유발. 첫 던전의 기본 층에는 다시 넣지 않는다.
- [ ] **마법 시스템**(현재 없음): 높이를 처음부터 주인공으로(아래로 흐르는 불 등). 도입하면
  시야·도달 판정은 `SightRules`를 그대로 쓴다.
- 액터 스탯(`SafeFallHeight`/`ClimbHeight`)의 출처는 **장비**이고 이미 구현돼 있다
  (`Equipment.cs` — 완충 부츠가 안전 낙하 높이 +2). 남은 축은 `ClimbHeight` 같은 이동 스탯뿐이다.
- 상세 개념: `docs/ARCHITECTURE.md` §5(시야). 낙하 곡선 수치는 `docs/SYSTEMS.md`가 소유한다.

## UI 방침 (확정)

- UI 이원화 확정: **화면공간 평면 = UI Toolkit, 월드 앵커/추종 = UGUI**. 상세·화면별 배치·Claude
  디자인 워크플로는 `docs/UI_ARCHITECTURE.md` (SSOT).

## 확정된 방향 (GDD v0.3.3)

> 요약본은 GDD 개정 때 낡는다 — 어긋나면 **`GDD.md` §10·§11이 정답**이다. 여기엔 네 줄만 둔다.

- **한 판 목표**: 첫 던전 폐 아케이드 복합타워를 **B2에서 8F까지 타고 올라가** 최상층 보스(감시자)를 쓰러뜨리고
  봉인이 풀린 옥상 출구로 정복한다. 도달만으로는 끝나지 않는다.
  **한 판 길이에는 목표치를 두지 않는다** — 밀도를 설계하고 시간은 결과로 받는다.
- **진행/죽음**: 로그라이트 메타 프로그레션 (죽으면 판 종료, 일부 해금 이월).
- **테마**: 아포칼립스 + 사이버펑크 / **세력이 나눠 가진 붕괴 도시**(v0.3.4) — 여러 세력이 붕괴한
  근미래 네온 도시를 조각내 점유하고, 플레이어는 **픽서 계약을 받아** 그 틈으로 들어간다.
  **미궁과 이상(異常) 현상은 삭제됐다** — 초자연 없이 전부 기술과 사람의 문제이며, 틸 신호색의
  근거도 서사가 아니라 **기능**(열림·통과·냉각)으로 다시 섰다.
  **설정에 방향성을 두지 않는다**: 예전 문구 "도시 밑의 미궁"은 모든 던전을 지하·하강으로 못박아
  기둥 1을 한 형태로 굳혔다. 상승·하강·평면은 **던전별 정체성**이고 방향 값의 출처는
  `DungeonCatalog`의 `direction:` 인자다. 판타지에서의 리스킨은 진행 중이다.
- **영웅**: **단일 원정자로 확정**(`SurvivorProfile`) — 직업/프리셋 없음. 정체성은 캐릭터가 아니라
  **장비**가 진다(숫자만 다른 캐릭터 선택은 전술이 아니라 난이도 선택이었다).
