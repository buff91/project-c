# 게임 기획서 (GDD) — *가제: Project-C*

> 아이소메트릭 다층 던전 크롤러 (Shattered Pixel Dungeon 계보 + 지형·원소·높이 상호작용 전투)
> 출시 플랫폼: 모바일 (iOS/Android, PC 동시 지원 고려) · 현재 개발/검증 타깃: PC 가로 · 엔진: Unity 2D · 아트: 픽셀아트
> 문서 상태: **v0.3.4** — 구현과 함께 다듬어가는 살아있는 문서
>
> 현재 구현 상태와 작업 순서는 각각 `docs/STATUS.md`, `docs/ROADMAP.md`가 SSOT다. 아래 §8은
> 초기 마일스톤의 역사적 기준선이며 현재 체크리스트로 사용하지 않는다.

---

## 1. 한 줄 콘셉트 (Elevator Pitch)

> 시야에 따라 조금씩 드러나는 다층 아이소메트릭 던전에서, 지형·불·얼음·높이를 전부 무기로 삼아 — **기름에 불을 붙이고, 얼려서 밀어 떨어뜨리고, 바닥을 무너뜨리며** — 싸우는 턴제 로그라이트.

> **왜 "높이차"를 앞세우지 않는가**: 높이차는 우리가 가진 **여러 전술 축 중 하나**다. 한 줄 콘셉트가
> 높이만 가리키면 상태이상·요소 반응·지형 상호작용이 부속품처럼 읽히고, 실제로도 "모든 전투의 답은
> 밀어 떨어뜨리기"로 수렴할 위험이 있다. 콘셉트는 **상호작용의 다양성**을 앞세우고, 높이는 그 목록의
> 첫 항목으로 둔다. (기둥 자체는 그대로 4개 — §2)

## 2. 핵심 기둥 (Design Pillars)

게임의 모든 결정은 이 기둥들을 기준으로 판단한다. (기둥은 적게 유지 — 아래 4개가 상한)

1. **입체 공간(Verticality)** — 공간 자체를 전술 자원으로 쓴다. **높이차는 여러 전술 축 중 하나**이며
   기둥 2(상태이상·요소 반응)와 **대등**하다 — 모든 전투의 답이 되어서는 안 된다.
   **건물형(포스트아포)에 맞춰 수직성의 어휘를 재정의(v0.3)**:
   - **층(floor)이 수직의 주 단위.** 층은 계단실·붕괴 구멍·**창문**·엘리베이터 통로 같은 **개구부(opening)** 로 이어진다.
   - **한 층 안의 높이차(elevation)는 엔진 능력으로 유지하되, 던전별 정책이 허용한 동기 있는 구조물**
     (지하철 승강장, 창고 메자닌, 컨테이너 더미, 잔해 언덕)에만 드물게 쓴다. 첫 던전은
     **한 층=하나의 이동 평면**이며 수직성은 층 전환·개구부·낙하로 표현한다. BG3식 연속 다높이는 지양.
   - **낙하는 이 공간성을 활용하는 여러 상호작용 중 하나** — 창문 밖으로 밀기, 바닥 붕괴, 개구부로 뛰어내리기 등.
2. **상호작용 & 상태이상(Interaction & Status)** — 전투의 깊이는 여기서 나온다.
   - 지형·오브젝트·적과 상호작용 (오브젝트 밀기/부수기/작동).
   - 상태이상: **폭발 / 빙결(Freeze) / 화상(Burn)** 등. 요소끼리 **반응**(불+기름=확산, 물+빙결, 전기 등).
3. **제한된 시야(Limited Sight)** — 모든 걸 보여주지 않는다. 정보의 부재가 곧 긴장.
4. **파밍 & 조합(Farming & Crafting)** — 판을 진행하며 자원을 모으고 아이템을 조합. 메타 프로그레션과 연결.

> 창발적 전술(Emergent Tactics)은 위 기둥들이 충돌해 저절로 생기는 결과 — 목표이자 검증 기준.

## 3. 타깃 & 범위

- **타깃 플레이어**: 로그라이크/픽셀아트 팬, 짧게 여러 판 즐기는 모바일 유저
- **1차 목표**: 학습 + 완성 가능한 세로형 프로토타입 (출시는 그 다음)
- **개발 인원**: 1인 (개인 개발)
- **범위 원칙**: **잔인할 정도로 좁게.** 첫 목표는 "한 층에서 이동·전투·아이템이 도는 것".

---

## 4. 게임플레이 개요

### 4.1 기본 루프

```
타이틀 → 허브(픽서 계약 수주 / 준비 / 원정지 선택) → 던전 진입 → 층 탐험(FOV로 조금씩 드러남) → 전투(일반 + 층간 낙하)
   → 아이템/자원 획득 → 계단/구멍으로 다음 층 → 반복 → 사망 시 처음부터
```

- 타이틀과 허브는 독립 씬으로 유지한다. 이후 프롤로그·세계관·스토리 연출은
  `타이틀 → (스토리 씬) → 허브` 사이에 삽입하며 허브 월드나 던전 씬에 종속시키지 않는다.

### 4.2 조작 (입력 추상화 — 현재 PC 우선)

- **빈 타일 탭**: `Visible`/`Explored` 타일과 현재 활성 층의 `MappedSilhouette` 타일을 목표로
  최단 경로 이동한다. mapped 목표까지 가는 경로의 일반 닫힌 문은 문 앞 접근 → **열기 1턴** →
  FOV 갱신 → 적 턴 → 인터럽트 평가 → 경로 재계획을 명시적으로 거쳐 통과한다. 이 과정에서 피해를 입거나
  새 적·새 아이템이 보이면 `피해 > 새 적 > 새 아이템` 우선순위로 즉시 자동 이동을 멈춘다.
  비밀문은 자동으로 열지 않고, 사다리는 발판까지만 이동한 뒤 자기 탭/Space의 명시적 행동으로 탄다.
- **겹친 층 탭**: 평상시는 화면에 실제 렌더된 현재 활성 층 타일을 우선한다. PC의 명시적
  `▲/◆/▼ 층 보기` 중에는 선택한 인접 층 조각이 우선한다. 어느 경우에도 활성 플레이 층·AI·미니맵은
  바뀌지 않는다.
- **적 탭**:
  - 근접 무기 → **인접 칸까지 이동 후** 공격 (붙어야 공격)
  - 원거리 → 조준 후 발사
- **오브젝트 탭**: 오브젝트 선택 시 **상호작용 버튼(들)이 팝업** (밀기 / 부수기 / 열기 / 줍기 등 문맥별)
- **높이 조준**: 다른 층 직접 사격·근접 공격은 금지한다. 실제 Hole을 사이에 둔 인접 층은 명시적
  층 보기에서만 광역 투척물(폭발물·냉각재·기름)의 바닥 목표를 고를 수 있다.
- **투척 조준**: 아이템을 고르면 현재 시야에서 실제로 던질 수 있는 타일 범위를 월드에 먼저 표시한다.
  숫자 안내만 띄워 플레이 영역을 가리지 않으며, Unknown·벽 뒤 칸은 범위 정보로 누설하지 않는다.
- **수직 보기(PC)**: 미니맵 아래 `▲/◆/▼` 레일로 실제 개구부를 통해 보이는 바로 위/현재/바로 아래
  층을 무턴으로 전환한다. HUD는 `현재 층 · 보기 층`을 함께 적고 카메라는 플레이어와 원격 조각을 한 프레임에
  남긴다. 다른 층을 보는 동안 이동·대기·상호작용·근접/일반 사격·회복은 잠기며, `◆`/Escape 또는 유효한
  층간 투척 뒤 현재 층으로 복귀한다. 개구부가 보이지 않는 방향 버튼은 비활성이다.
- **PC 대응 (현재 유일한 개발·검증 타깃 — §7)**: 위 규칙은 포인터에 그대로 얹히고(탭 = 클릭),
  키보드는 **같은 액션을 다른 문법으로** 부른다 — 이동 = 방향키/WASD(**화면 기준**, 시점 회전 보정),
  인접 상호작용 = Space(적 공격 > 문 > 허브 오브젝트 > 폭발통 순), 대기 = X, 시점 회전 = Q/E,
  게임 메뉴 = ESC, 액션 휠 = **Tab 홀드 전용**. 캐릭터 클릭은 월드 선택만 하며 휠을 고정하지 않는다.
  Ctrl/Cmd 같은 범용 modifier는
  스크린샷·복사 등 OS 단축키와 충돌하므로 액션 바인딩에 쓰지 않는다.
  키가 여는 것은 탭이 여는 것과 **같은 액션이어야 한다** — 갈라지는 순간 §12의 입력 추상화가 이름만 남는다.
- 턴제이므로 실시간 반응속도 부담 없음 → 모바일에 적합
- ⚠️ **PC/모바일 동시 지원 고려** → 입력 추상화 필요 (아래 §12 참고)

---

## 5. 핵심 시스템

### 5.1 다층 격자 (Foundation)

- 논리 좌표: `GridPos { int x, y, int elevation }`
  - `elevation` = **연속적인 높이값**. "층 번호"가 아니라 한 층 안에서도 높이차를 표현할 수 있다(계단·경사·구조물).
    실제 생성 여부는 던전별 정책이다. 첫 던전의 이동 가능한 타일은 각 층의 기준 elevation
    (`LocalHeight == 0`)만 사용한다. `ElevationsPerFloor=4`는 층간 간격·정렬·낙하 계산을 위해 유지한다.
  - "층(floor)"은 elevation을 크게 묶은 논리적 구획이자 **수직의 주 단위**. 층 사이는 계단실·구멍·창문 등 개구부로 잇는다.
- **진행 순서 ≠ 고도 (v0.3.2 확정 규약).** 던전은 **올라갈 수도, 내려갈 수도, 평면일 수도** 있고,
  한 던전 안에서도 **올라갔다 떨어지거나 내려갔다 올라오는** 경로가 가능하다.
  - 따라서 **"몇 번째 층인가"(진행 지수)는 elevation의 함수가 아니다.** `1F → 3F → 2F → 5F` 같은
    경로에서 2F의 진행 지수는 고도로 역산할 수 없다.
  - **진행 지수는 생성기가 경로를 깔면서 각 층에 부여하는 데이터**여야 하며,
    난이도·콘텐츠 규칙(적 혼합·휴식처·탈출구·장비 드랍·숨은 방·구간 변주·보스)은 전부 이 값을 키로 쓴다.
    난이도는 **얼마나 나아갔는가**를 따르지, 얼마나 높은가/낮은가를 따르지 않는다.
  - `Elevation`은 정렬·낙하·시야·공간 연결 전용이며 난이도와 무관하다. 두 축을 섞으면
    상승 던전에서 난이도가 조용히 무너진다(실제 결함 — `docs/ARCHITECTURE.md` 참조).
- 화면 좌표: 아이소 변환 + `elevation` 높이 반영 (정렬 우선순위도 elevation 기반)
- 같은 층 내 높이를 허용한 원정지만 계단/경사로에서 elevation이 변하고 이동 비용·시야·조준에 반영한다.
  첫 던전에는 이 이동을 생성하지 않는다.
- **렌더링되는 높이 ≠ 시뮬레이션되는 범위** 를 분리 (성능, §5.7 참고)

### 5.2 시야 / 안개 (FOV)

- 타일 상태: `Unknown` / `Explored` / `Visible`
- `MappedSilhouette`는 네 번째 FOV 상태가 아니라 **별도의 지도 지식 축**이다. 현재 활성 층의 일반
  토폴로지만 공용 저대비 실루엣으로 보여 주며, 액터·아이템·프롭과 특수 타일의 정체·상태는 공개하지 않는다.
  따라서 한 타일은 `Unknown`이면서 `MappedSilhouette`일 수 있고, 이때 실루엣은 이동 목표로 클릭할 수 있지만
  전술 정보는 계속 Unknown이다. 공용 바닥으로 접힌 미확인 `StairsUp/Down`은 밟는 즉시 층이 바뀌는 정체가
  실제 FOV로 확인될 때까지 자동 경로가 진입하지 않는다. 비활성 층에는 적용하지 않아 기존 개구부 국소
  미리보기만 남긴다.
- 알고리즘: **Recursive Shadowcasting**
- 층 규칙:
  - 플레이어 층: 일반 FOV
  - 아래층: **구멍(Hole) 타일을 포털 삼아** 그 주변 반경만 Visible
  - 위층: 기본 차폐, 뚫린 곳 아래에서만 보임
- 수직 이동과 수직 시야는 분리한다. 층내 높이를 허용한 원정지에서만 같은 층의 계단은 걸어서 통과하고,
  사다리는 첫 입력에 발판에 부착한 뒤 두 번째 자기 탭/상호작용으로 elevation을 이동한다. 첫 던전에는
  이 둘을 일반 지형으로 생성하지 않는다. `StairsUp/Down`은 밟는 즉시 활성 던전 층을 전환하며 다른 층을
  투시시키지 않는다. 실제 `Hole`·개구부만 위·아래 양방향 시야 포털이다. 색은 보조 신호일 뿐이며,
  계단=연속된 발판, 사다리=두 착지 높이에 딱 맞춘 세워진 레일, 층 전환=어두운 계단 아치+목적 층 표지,
  개구부=깨진 바닥+아래 공간이라는 실루엣으로 먼저 구분한다.
- 수직 보기는 passive 개구부 FOV의 부분집합만 밝힌다. **전체 층 공개나 순간이동이 아니며**, 대상 층을
  시뮬레이션 활성화하지 않는다. 층간 투척 사거리는 `플레이어→현재 쪽 endpoint 맨해튼 + 개구부 통과 1 +
  반대 endpoint→목표 맨해튼`이고 두 평면 구간의 시야선·단단한 목표 바닥이 모두 성립해야 한다.
- **창문(Window)은 수평 시야 포털이다(v0.3).** 온전한 유리는 이동은 막지만 시야는 통과한다(`BlocksSight=false`) — 창문 너머 방·바깥을 볼 수 있고, 깨면(→`WindowBroken`) 통로가 된다. 높이 인식 시야선이 같은 층 창문 투시를 그대로 처리한다.
  - **실내 창문**(양옆이 같은 높이 바닥): 깨면 **방 사이 수평 통로** — 문과 형제. 다른 진입로/사선을 연다.
  - **외벽 창문**(창밖이 허공/아래층): 깨고 넘거나 밀면 **낙하**로 이어진다(§5.3). 온전한 유리는 넉백을 막는다(=벽).
- 적의 피해 숫자·사망 안내·적중 수는 `Visible`인 대상만 공개한다. 시야 밖 전투 결과는 시뮬레이션과 전적에는 반영하되 HUD 정보로 누설하지 않는다.
- 표현: `Visible`은 실제 표현, `Explored`는 기억 표현, `Unknown + MappedSilhouette`는 공용 토폴로지
  실루엣, 나머지 `Unknown`은 안 그림. 자유 카메라와 미니맵은 현재 활성 층의 mapped 경계를 쓴다.

### 5.3 지형·층간 상호작용 & 낙하

> **"시그니처" 표기를 뺀 이유(§1 개정과 동일)**: 층간 전투는 SPD와 우리를 가르는 차별점이지만,
> 그렇다고 **가장 강한 전술**이거나 다른 축보다 우선하는 것은 아니다. 이 절은 §5.5(상태이상·요소 반응)와
> **대등한 전술 축 하나**이며, 둘이 맞물릴 때 나오는 결과(얼려서 밀어 떨어뜨리기)가 목표다.

- 낙하 유발 트리거 (모두 `TryFall()` 하나로 수렴):
  - 넉백 공격으로 구멍으로 밀기
  - 약한 바닥(Weak Floor) 붕괴
  - 함정 발판 / 밀 수 있는 오브젝트
  - **깨진 창문 밖(허공)으로 밀기/뛰어내리기** — 창문을 깨면 개구부가 되고, 그 너머가 허공/아래층이면 낙하로 수렴한다.
- 낙하 처리: 아래로 첫 단단한 바닥 탐색 → 낙하 층수 계산 → 낙뎀(누적 곡선) → 착지 지점 적 있으면 양쪽 데미지
- 플레이어도 동일 규칙 적용 (실수로 떨어질 수 있음 = 긴장감)
- **의도적 낙하는 확인 입력을 거친다.** 보이는 구멍의 첫 탭은 충돌층·진행상 의미·장비까지 반영한
  예상 피해를 고정할 뿐 턴과 이동을 쓰지 않는다. 같은 구멍 재클릭 또는 Space에서만 가장자리까지
  접근해 낙하한다. 포인터 hover는 같은 정보를 미리 보여 주는 선택 보조이며 터치의 필수 입력이 아니다.
  다른 타일/행동 또는 Escape는 확인을 취소한다.

> **낙하의 의미는 던전 방향에 따라 뒤집힌다 (v0.3.2)**: **중력은 뒤집히지 않는다.** 하강 던전에서
> 낙하는 *전진 지름길*("계단 대신 뛰어내리기")이지만, **상승 던전(첫 던전 아케이드 타워)에서는 진행 역행**이다.
> 대신 **한두 층을 되짚어 내려가 위험을 끊는 후퇴 수단**이 된다. 고도가 진행 축이 아닌 던전(§10.2)에서는
> 셋 중 어느 쪽도 아니고 **진행과 무관한 지형 위험**일 뿐이다. 익스트랙션·배고픔과 맞물려 오히려 새
> 긴장을 만든다. **서스펜션 부츠(안전 낙하 +2)는 폐기되지 않고 의미만 바뀐다**: 규칙(`FallRules`)은 방향과
> 무관하게 하나이고 **레벨 디자인상의 용도만 달라진다**(지름길 / 후퇴 / 지형 위험).

- **빠른 후퇴는 낙하가 아니라 탑승이다.** 예전에는 "위험하면 통로로 뛰어내려 빠르게 하강한다"고 적었는데
  **구현이 이걸 수치로 부정했다** — 누적 곡선상 3층 낙하는 12이고 원정자 HP 는 10이라, 서스펜션 부츠를 껴도
  9(HP 1 잔존)다. 여러 층 자유낙하는 탈출이 아니라 자살이다. 그래서 층을 건너뛰는 복귀는 **엘리베이터**가
  맡는다: 던전당 한 대, 탑승구는 **보스 아레나 바로 앞 층**이라 올라가는 길에 **멈춰 있는 것을 먼저 보게**
  되고, 보스를 잡아 건물 전원이 들어오는 순간 그것이 움직인다 — 건물이 깨어나는 장면이 곧 복귀 동선의
  해금이다. **진행의 반대 방향으로만** 가는 복귀 전용이며(진행 방향으로 태우면 계단을 건너뛰는 지름길이
  되어 페이싱이 무너진다), 고도가 진행 축이 아닌 던전에는 놓지 않는다.
  규칙은 `ElevatorShaftRules`, 수치·배치는 `docs/SYSTEMS.md` 「엘리베이터」.

### 5.4 전투 (기본)

- 턴제: 플레이어 1행동 → 몬스터 1행동
- 적 시체는 결과 인지를 위해 기본 3턴간 남아 점점 흐려지고, 이후 월드 오브젝트·탭 대상·전투 목록에서 완전히 제거된다.
- 근접(**반드시 인접**) / 원거리(투사체 궤적) / 넉백
- HP, 간단한 스탯
- 타격 순간은 공격자 짧은 돌진 → 대상 스쿼시/플래시 → 픽셀 충격 버스트·피해 숫자 순으로 읽힌다.
  물리/화염/냉기/낙하·충돌은 색과 충격 크기, 화면 흔들림 강도가 서로 달라야 한다.
- 카메라 흔들림은 짧고 감쇠하며 조작을 막지 않는다. 시야 밖 적의 타격 연출은 생성하지 않는다.
- 낙하는 전투 요소 중 하나 — 아래층으로 **이동하는 수단으로도** 사용 가능 (계단 대신 뛰어내리기, 낙뎀 감수).
  하강 던전에서는 전진 지름길, **상승 던전에서는 후퇴 수단**, 고도가 진행 축이 아닌 던전에서는
  진행과 무관한 지형 위험이다(§5.3 주석). 여러 층을 건너뛰는 복귀는 낙하가 아니라 엘리베이터다.

### 5.5 상태이상 & 요소 반응 (전투 깊이의 핵심)

- **지속 상태이상(대상에 붙고 턴을 센다)**: **화상(Burn)** · **빙결(Freeze)** · **중독(Poison)**.
  **폭발(Explosion)과 감전(Shock)은 지속 상태가 아니라 즉발 반응**이며, **기름/젖음은 대상이 아니라
  타일이 갖는 원소 상태**다 — 셋을 한 목록에 섞어 두면 "감전 몇 턴?" 같은 없는 규칙을 찾게 된다.
- 화상은 따뜻한 몸 틴트와 발밑 주황 불꽃 고리, 빙결은 차가운 몸 틴트와 청록 결정 고리로
  지속 상태를 보여 준다. 최초 부여·지속 연장·불↔얼음 상쇄는 서로 다른 짧은 팝업/버스트로 알린다.
- 상태 아이콘도 FOV 정보 차단을 따른다. 보이지 않는 적의 상태를 화면 효과로 추측할 수 없어야 한다.
- **요소 반응 규칙** — 시스템끼리 반응해 창발적 전술 생성:
  - 불 + 기름/가스 → 확산·폭발 (반대로 젖은 바닥에는 기름이 붙지 않는다)
  - 물/젖음 + 빙결 → **이어진 웅덩이 전체** 결빙, 이동 봉쇄
  - 젖음 + 감전 → **이어진 웅덩이 전체** 통전(광역). 물이 도체라 "적을 먼저 웅덩이로 몬다"가 셋업이 된다
  - 젖은 칸 + 화상 → **소화**(불이 꺼진다). 반대로 불 폭발은 웅덩이를 증발시킨다 — 물은 방어이자 소모품이다
  - 화상 + 폭발통 → 연쇄 폭발
- 구현: 타일/엔티티에 **상태 플래그 + 지속턴**, 매 턴 반응 규칙을 평가하는 **반응 테이블**(데이터화)
- 낙하·오브젝트와 결합 → "적을 얼려서 밀어 떨어뜨리기" 같은 콤보

### 5.6 파밍 & 조합 (Crafting)

- 던전 진행 중 자원/재료 수집(파밍)
- 재료 조합으로 소모품/장비 제작 (예: 기름+병=화염병)
- 메타 프로그레션과 연결 (다음 판으로 이어지는 레시피 해금 등)
- **백팩은 6×4 멀티슬롯 격자**다. 소형 1×1, 연료통·투척 볼트·귀환 비컨 1×2,
  대형 시제품 코어 2×2처럼 아이템 하나마다 실제 면적을 점유한다.
- 모바일 프로토타입은 큰 아이템 우선의 결정론적 자동 정리를 사용한다. 공간이 없으면
  월드 아이템을 획득하지 않고 바닥에 남기며, 출정 백팩에 넣지 못한 물품도 창고에 둔다.
- 백팩의 같은 종류도 인스턴스별 면적을 차지한다. 허브 창고는 대량 보관용이므로 종류별 중첩을 유지한다.
- **허브에서 원정 물품을 직접 준비한다.** 창고와 6×4 출정 백팩을 나란히 보여 주며,
  모바일은 선택 후 반대편 빈 공간 탭, PC는 버튼 또는 드래그로 한 개씩 옮긴다.
- 기본 지급품은 출정 백팩에 잠금 상태로 먼저 배치한다. 공간이 부족해지면 선택 물품을
  삭제하지 않고 창고로 되돌린다. **지급품은 하나다** — 영웅 선택이 사라졌다(§11 "단일 원정자").
- 던전에는 기본 지급품과 출정 백팩에 넣은 물품만 반입한다. 진입 시 선택 물품은 창고 메타에서
  원정 인벤토리로 이동하며, 선택하지 않은 창고 물품은 다음 원정을 위해 그대로 보존한다.

### 5.7 몬스터 AI & 활성화/컬링 (성능 핵심) ⭐

> FOV만큼이나 중요. **모든 몬스터를 매 턴 연산하면 안 된다.**

- **활성 반경(Activation Radius)**: 플레이어 주변 일정 범위 + 같은 방/시야 내 몬스터만 "활성" → 매 턴 사고
- 비활성 몬스터: 정지 또는 저비용 "휴면" 상태 (간헐 갱신)
- 다층: 보이는 층이라도 **구멍 근처 등 상호작용 가능한 몬스터만** 활성
- **몬스터 생성(spawn) 로직**: 절차생성 시 배치 규칙 + 진행 중 추가 스폰 규칙 (난이도·진행 지수 연동)
- 행동 결정: 단순 상태머신(순찰 → 추격 → 공격 → 도주)부터 시작, 필요 시 유틸리티 AI로 확장

### 5.8 아이템 / 던전 생성

- 무기, 소모품(폭탄=바닥 파괴 등), 열쇠, 조합 재료
- 절차적 던전 생성(룸-앤-코리더 또는 BSP) + **던전별 층내 높이 정책**. 첫 던전은 평탄,
  허용한 원정지만 계단·경사·메자닌을 배치한다.
- **숨은 방**: 첫 던전 10개 층 중 **보스 층을 뺀 진행 층**에서 seed로 고른 3개 층에 메인 진행과
  무관한 막다른 방을 둔다(층 라벨이 아니라 진행 지수로 센다 — §10.1 용어 규약).
  입구는 벽으로 위장해 이동·시야를 차단하고, 인접하면 작은 균열과 `수상한 벽 조사` 상호작용이
  나타난다. 조사하거나 3×3 폭발을 맞히면 다시 닫히지 않는 열린 통로가 되며, 안에는 코어 파편/시제품 코어 1개를 보장한다.
  공개 전 비밀문 좌표는 독립된 mapped 셀로 그리지 않고 기존 방의 평범한 외곽 경계에 묻는다. 비밀방
  footprint와 보상도 FOV·미니맵·`MappedSilhouette`에 노출하지 않는다. 공개 후 통로와 방 footprint를
  mapped 지도에 추가하며, 보상은 실루엣이 아니라 일반 FOV 아이템 공개 규칙을 따른다.

### 5.9 아트 파이프라인 (교체 자유)

- 로직 ↔ 비주얼 분리
- **ScriptableObject** 로 "타입 → 스프라이트/애니" 매핑
- 네이밍 규칙 + Sprite Atlas
- 픽셀아트 임포트 **프리셋** (Point filter, No compression, PPU 통일, Pixel Perfect Camera)

### 5.10 장비 (정체성을 지는 것)

> §11이 직업제를 걷어내면서 **정체성은 캐릭터가 아니라 장비가 진다**로 확정했다. 그 순간 장비 설계
> 규칙은 아이템 하위 항목이 아니라 **기둥급 결정**이 되므로 여기에 적는다. 가격·드랍 확률처럼
> 실플레이로 흔들릴 임시 수치는 `docs/SYSTEMS.md` 「장비」가 소유한다.

- **어떤 장비도 공격력을 올리지 않는다.** 장비가 바꾸는 것은 숫자가 아니라 **행동 규칙**이다 —
  근접 사거리(직선 2칸) · 명중 시 넉백 1칸 · 받는 물리 피해 −1 · 안전 낙하 높이 +2 ·
  **원거리 사격 자체(사거리 5)**.
  영구 스탯 강화를 금지해 놓고(§11) 장비로 공격력을 주면 **같은 인플레가 이름만 바꿔 돌아온다.**
  방어 보정도 물리 피해에만 걸고 상태이상 틱은 막지 않는다 — 기둥 2를 장비로 무력화하지 않기 위해서다.
- **원거리는 기본으로 쥐어 주되 연사할 수 없다.** 누구나 무제한으로 쏘던 시절에는 카이팅이
  언제나 최적해라 접근전이 성립하지 않았다. 그렇다고 탄약 아이템으로만 막으면 반대 문제가
  생긴다 — 탄이 떨어진 판은 원거리가 통째로 사라져 **플레이어가 그 축을 저울질할 기회를
  잃는다**. 그래서 모두가 내장 이미터를 쥐고 시작하되 충전이 시간으로 차게 했다: 한 발 쏘고,
  차는 동안 붙어서 싸우고, 다시 한 발. "붙을까 떨어질까"가 매 턴의 결정이 되는 지점이 여기다.
  아크 캐스터는 이 축을 **여는** 게 아니라 **깊게** 만드는 상위 티어이며(사거리·충전·재충전),
  같은 이유로 원거리 무기는 근접 사거리를 늘리지 않는다.
  수치·구현은 `docs/SYSTEMS.md` 「원거리 사격」.
- **슬롯은 둘 — 무기 1 + 보조 1.** 무기는 "사거리 대 넉백", 보조는 "안전 대 낙하 전술"로 갈려
  고르는 행위 자체가 이번 판의 전술 선언이 된다. 슬롯이 늘면 조합이 아니라 **누적**이 되고,
  그러면 "무엇을 포기했는가"가 사라진다.
- **장착 = 반입이다.** 장착한 장비는 창고에서 **꺼내서** 나가므로 소모품과 똑같은 위험에 놓인다 —
  죽으면 잃고, 살아 돌아와야 돌려받는다(익스트랙션 규칙). 창고에 남긴 예비 장비는 안전하다.
  무엇을 걸고 나갈지가 판돈인 게임에서 장비만 면제하면 판돈이 가벼워진다.
- **장착한 장비는 백팩 면적을 쓰지 않는다.** 대신 아직 끼지 않은 장비는 면적을 차지한다
  (전광판 방패는 2×2). 던전에서 주운 장비를 들고 나올지가 백팩 압박(§5.6)과 직접 맞물린다.
  빈 슬롯일 때만 자동 장착한다 — 낀 것을 말없이 갈아치우면 "더 좋은 걸 주웠나?" 판단을 뺏는다.
- **입수 경로는 둘**: 대장간에서 **골드로 제작**(만든 장비는 창고에 남는다), 또는 던전 드랍.
  드랍은 초반 구간에는 나오지 않고 층당 최대 하나이며, **대장장이를 구출하기 전에는 아예 나오지 않는다**
  — 쓸 수도 고칠 수도 없는 물건을 줍게 하지 않는다.
- **제작비는 골드의 주 목적지다.** 직업 해금이 사라지며 비게 된 골드 싱크를 여기로 옮겼다.
  생환 보상이 갈 곳이 소모품뿐이면 골드가 남아돌고, 그러면 생환 자체가 판돈이 아니게 된다.

---

## 6. 아트 & 연출 방향

- 픽셀아트, 아이소메트릭 스프라이트 — **4방향 기본, 8방향도 적극 검토** (입체감·조준 표현에 유리, 아트 비용과 트레이드오프)
- **네온 아포칼립스 디오라마 (v0.3.3)**: 차가운 청흑 void 위에 **콘크리트 회색·벽돌 적갈·녹 주황**을
  두고, 국소 광원은 **두 축**을 쓴다 — **웜**(형광등/비상등/드럼통 불의 호박색)과
  **쿨 네온**(간판·홀로·자판기의 마젠타/시안). **열림·통과·냉각의 청록(틸)은 판정 신호색으로 남는다** —
  시안 네온 광원과 역할을 섞지 않는다(틸=Hole·해금된 출구·게이트·빙결 판독, 시안/마젠타 네온=장식 광원.
  네온은 넓은 면적을 스프라이트에 굽지 않고 광원 오버레이·간판 소품과 액터의 국소 기술 악센트로만 쓴다).
  **적 액터는 차콜·건메탈이 몸체를 지배**하고, 녹·갈색은 재료색이 아니라 가장자리 마모에만 제한한다.
  시안·마젠타는 센서·케이블·기업 식별 스트립 한두 곳에만 두며, 적대 표식은 작은 적색 IFF 한 점으로
  통일한다. 전신 네온 도색이나 갈색·황토색이 주색인 로스터는 이 계약을 어긴다.
  팔레트 *원리*(청흑 바탕 + 국소 광원 + 신호색 1개)는 유지하고 **광원축 하나와 재료 어휘가 늘어난다.**
  풍화 3종(녹 · 균열 · 잡초 잠식)에 **죽은 테크**(꺼진 전광판·깨진 홀로 패널·늘어진 케이블)를 더한 것이
  폐허의 기본 텍스처다. 방향·레퍼런스 SSOT는
  `docs/art-direction/project-c-postapoc-art-direction-v1.md`(v0.3.3 네온 개정 주기 포함).
- 허브는 청부업자들의 거점 — 드럼통 불·비상등의 로컬 광원으로 휴식처임을 즉시 읽히게 하되, 던전은
  제한 시야와 상태이상 색 판독을 우선한다. 조명색은 바닥 원본에 굽지 않고 별도 오버레이로 분리한다.
- **Torchstone 공통 18색 팔레트**(기술 기반): 던전·허브·UI는
  `Assets/_Project/Art/Source/Aseprite/project-c-torchstone.gpl`의 아래 18색을 공유한다.
  런타임 던전 역할색은 `ProjectCEnvironmentCatalog`가 이 토큰을 가리킨다.
  구간별 변주는 새 팔레트를 만들지 않고 같은 팔레트 안에서 사용 비율과 명도 단계를 바꾼다.

  | 계열 | 색 |
  |---|---|
  | 청흑 배경 | `#05070C` / `#07090E` / `#0A0D13` |
  | 돌 프레임 | `#4A4038` / `#98866F` / `#CFC0AE` |
  | 토치 / 골드 | `#9A6B22` / `#FFBD41` / `#FFD554` |
  | 본문 / 보조 | `#97907E` / `#EADFC8` |
  | HP / 화염 | `#45100B` / `#D8452A` |
  | 틸 / 얼음 / 물 | `#14343A` / `#1C4347` / `#4FA7A0` / `#9ADFE8` |
  | 독 / 경험치 | `#7FB241` |

  공통 표면 계조는 웜 그레이·토프를 뼈대로 두고 재료 어휘만 콘크리트·벽돌·녹으로 읽으며,
  void/FOV는 청흑색이다. 토치 골드는 광원·안전 경로·현재 목표에, 틸은 Hole·게이트에 쓴다.
  (**에셋이 어디까지 리스킨됐는지는 GDD가 아니라 `docs/STATUS.md` 「아트 방향」이 소유한다** —
  여기는 방향만 정한다.) 층내 단차를 허용한 원정지는 표면 색상군을 바꾸지 않고
  명도와 타일 반높이 측면 두께(128-레짐 화면 32px)로 구분한다. 첫 던전은 raised 바닥을 생성하지 않는다.
  - **포스트아포 전환**: 이 18색 토큰(hex)은 대체로 유지하고 *역할*만 재매핑한다 — 석재군 →
    콘크리트/벽돌/녹, 토치 골드 → 비상등/네온, 틸 → 열림·통과/냉각. 재료 어휘 교체는 리스킨 표 참조.
  - **사이버펑크 전환(v0.3.3)**: 쿨 네온(마젠타/시안) 광원색과 액터 아이덴티티 램프(피부·머리)는
    코어 18색을 대체하지 않고 `.gpl`에 **append**한다(코어 순서 불변, 정확한 색은 `.gpl`이 소유).
- 바닥은 캐릭터보다 낮은 대비, 벽은 콘크리트/벽돌 덩어리와 좌상단 광원, 캐릭터는 **2.5~3등신**의
  큰 머리·스트리트웨어+장비 실루엣을 기본으로 한다(아군은 얼굴 노출·컬러 헤어가 아이덴티티, 적은 익명 —
  상세는 `docs/art-direction/project-c-actor-appeal-restyle-v1.md`).
  작은 모바일 화면에서도 역할과 상호작용 대상을 먼저 읽는다.
- **제작 파이프라인**: AI로 에셋 초안 생성 → **Aseprite로 다듬기 + 애니메이션 프레임 처리** → Unity 임포트 (프리셋 적용). Aseprite `.ase` → 스프라이트시트/JSON 워크플로 정립 필요.
- 낙하 연출: 떨어지는 애니메이션 + 카메라 살짝 아래 팬 + 흔들림
- 높이 조준 연출: 위/아래 보는 포즈 + 투사체 포물선 + 타겟 마커

---

## 7. 기술 스택

| 항목 | 선택 |
|------|------|
| 엔진 | Unity (2D, Isometric Tilemap) |
| 언어 | C# |
| 렌더 | Pixel Perfect Camera, Custom Axis Sorting |
| 데이터 | ScriptableObject 중심 |
| 출시 플랫폼 | iOS / Android (PC 동시 지원 고려) |
| 현재 개발·검증 타깃 | PC 가로 |

---

## 8. 초기 개발 로드맵 (역사적 기준선)

> 원칙: **각 단계는 "플레이 가능한" 상태로 끝난다.** 앞 단계 없이 뒷 단계로 점프 금지.
> 아래 체크박스는 최초 범위 정의를 보존한 것이며 현재 진행 상태를 나타내지 않는다.
> 최신 완료/진행/보류 상태는 반드시 `docs/ROADMAP.md`를 본다.

### M0 — 토대 (지금 여기)
- [ ] 다층 격자 데이터 구조 (`GridPos`, 타일맵)
- [ ] 아이소 좌표 ↔ 화면 좌표 변환
- [ ] 탭 → 격자 좌표 역변환
- [ ] 정렬(Sorting) 처리

### M1 — 단일 층 크롤러 (여기서 대부분 좌절함 → 완주가 목표)
- [ ] 격자 이동 (A* 경로)
- [ ] 턴 매니저 (플레이어/몬스터 순환)
- [ ] 기본 전투 (근접 + HP)
- [ ] 절차적 던전 생성 (단일 층)
- [ ] 아이템 1~2종 + 인벤토리

### M2 — 시야 & 몬스터 AI
- [ ] FOV (Shadowcasting) + 안개 3상태
- [ ] tint/alpha 렌더링
- [ ] **몬스터 활성화/컬링 시스템** (활성 반경, 휴면) ⭐
- [ ] 몬스터 상태머신(순찰→추격→공격) + 스폰 로직

### M3 — 입체 공간 & 다층 이동
- [ ] 같은 층 내 높이차(elevation) — 계단/경사로
- [ ] 계단으로 층 이동 (한 번에 한 층/범위만 활성)
- [ ] **내려갈 때는 뛰어내리기(낙하)로도 가능** (낙뎀 감수)
- [ ] 구멍(Hole) 타일 → 아래층 부분 가시화

### M4 — 지형·층간 상호작용
- [ ] `TryFall()` 낙하 + 낙뎀 곡선
- [ ] 넉백 / 약한 바닥 / 폭탄 트리거
- [ ] **상태이상 시스템** (화상/빙결/폭발) + 요소 반응 테이블
- [ ] 오브젝트 상호작용(밀기/부수기) + 상호작용 버튼 UI
- [ ] 낙하 연출

### M5 — 콘텐츠 & 폴리시
- [ ] 몬스터/아이템 다양화, 밸런스, UI, 사운드
- [ ] 아트 에셋 교체 테스트 (파이프라인 검증)

---

## 9. 리스크 & 대응

| 리스크 | 대응 |
|--------|------|
| 다층 시뮬레이션 성능/복잡도 폭발 | "보이는 층 ≠ 활성 층" 분리, 층간 상호작용은 구멍 기반으로 **제한** |
| 아이소 정렬(sorting) 지옥 | floor 우선 + (x+y) 정렬 규칙 초기 확립 |
| 픽셀아트 에셋 재작업 비용 | 로직/비주얼 분리 + 프리셋 초기 도입 |
| 범위 과욕(1인 개발) | 마일스톤 순서 엄수, M1 완주 최우선 |
| 층간 조준 UX 난해 | 타겟 마커 + 층 표시로 명확화, 상호작용 최소화부터 |

---

## 10. 확정된 방향 (v0.3 / v0.3.2 / v0.3.3 / v0.3.4)

> **v0.3 (2026-07-24)**: 테마를 정통 판타지 → **포스트 아포칼립스 / 이상 미궁**으로 전환.
> **v0.3.2 (2026-07-25)**: 첫 던전을 **폐병원 / 상승 구조**로 확정하고 미궁 설정에서 방향성을 제거.
> **v0.3.3 (2026-07-30)**: 테마를 **아포칼립스 + 사이버펑크 네온 미궁**으로 승격("SF 얇게" 조항 폐기),
> 첫 던전을 폐병원 → **폐 아케이드 복합타워**로 재설정(상승 구조·10층·용어 규약은 불변).
> **v0.3.4 (2026-08-01)**: **미궁과 이상(異常) 현상을 세계에서 삭제한다.** 초자연 축을 걷어내고
> **세력이 나눠 가진 붕괴 도시 + 픽서 청부**로 바꾼다 — 플레이어는 탐사자가 아니라 **계약을 받는
> 청부업자**다. 시스템 4기둥 · 보스 역할 · 한 판 구조 · 던전 토폴로지는 여전히 불변.
> 실제 에셋 컷 + Play 검증은 로컬.

- **한 판 목표**: **첫 던전 최상층 보스 처치 후 출구 정복** — 도달만으로 끝나지 않으며,
  보스를 쓰러뜨려 봉인을 해제하고 출구를 선택해야 승리한다.
- **진행/죽음**: **메타 프로그레션** — 죽으면 그 판은 끝(로그라이트)이지만 일부 해금·성장이 다음 판으로 이어짐. 재도전 동기 확보.
- **테마**: **아포칼립스 + 사이버펑크 / 세력이 나눠 가진 붕괴 도시** (v0.3.4 개정) —
  붕괴한 **근미래 네온 도시**를 여러 **세력**이 조각내 점유하고 있고, 그 사이의 무주공산에
  값나가는 것들이 남아 있다. 플레이어는 **픽서에게 계약을 받아 그 구역에 들어가는 청부업자**다.
  이 세계에는 빔 병기·홀로 간판·전광판·드론이 **일상 기술로 존재했다** — 다만 붕괴 이후라
  대부분 죽어 있고(꺼진 네온·방전된 셀), 살아 있는 기술은 희소 자원이다.
  **초자연은 없다. 전부 기술과 사람의 문제다.**
  몬스터/요소/아이템 리스킨은 `docs/art-direction/project-c-postapoc-reskin-table-v1.md`.
  - **구 조항 폐기 기록 (v0.3.4)**: **"이상(異常) 미궁"과 "기술로 설명되지 않는 별개 축"을 삭제한다.**
    미궁은 *왜 그 안에 들어가는가*를 "그냥 미궁이니까"로 답하게 만들어 동기를 무료로 만들었고,
    초자연 축은 사이버펑크 세계의 인과(누가 무엇을 원해서 나를 보냈나)와 계속 겉돌았다.
    이제 **동기는 계약**이고, 위험은 세력·기계·구조물이라는 **설명 가능한 출처**를 가진다.
  - **⚠ 이 삭제가 건드리는 것 — 틸 신호색의 근거.** 예전에는 "틸 = 이상(異常) 현상"이라는
    **서사적** 근거로 예약돼 있었다. 초자연이 사라졌으므로 근거를 **기능**으로 다시 세운다:
    **틸 = "열림·통과·냉각"의 판정 신호**(Hole·해금된 출구·게이트·빙결). 화면에서 갈라야 하는
    이유는 그대로다 — 쿨 네온(시안/마젠타)은 장식 광원이라 같은 색역에서 겹치면 판독이 무너진다.
    바뀐 것은 **왜 예약하는가**이지 **무엇을 예약하는가**가 아니다(§6 · `docs/UI_DESIGN_SYSTEM.md`).
  - 구 조항 폐기 기록 (v0.3.3): v0.3의 **"약간의 SF는 얇게"**는 삭제됐다. 생성 프롬프트의
    `cyberpunk neon` 네거티브도 함께 해제한다(네온 *과잉* 억제는 §6의 광원 오버레이 규칙이 대신 진다).
  - **설정에 방향성을 두지 않는다(v0.3.2 개정, v0.3.4에서도 유지).** 이전 문구는 "도시 **밑에**
    미궁이 자라났다"였는데, 이는 모든 던전을 지하·하강으로 못박아 **기둥 1을 한 형태로 굳힌다**.
    각 원정지가 상승·하강·평면 중 무엇인지는 **던전별 정체성**으로 남긴다.
  - **용어는 바꾸지 않는다.** `원정자`·`원정지`는 코드·세이브·UI 문자열에 널리 박혀 있고
    "계약을 받아 나간다"와 의미가 충돌하지 않는다. 코드 ID `forgotten-catacombs`를 유지한 것과
    같은 관례다(리스킨 표 §0-a) — **정체성 교체가 식별자 교체를 요구하지 않는다.**

- **플레이어의 자리 — 픽서 청부 (v0.3.4)**: 한 판은 **계약 수주 → 진입 → 목표 달성 → 생환**이다.
  - **기존 의뢰(현상금) 시스템이 곧 이 축이다.** `BountyRules`/`BountyMetric`과 허브 의뢰 게시판이
    이미 "조건을 걸고 보상을 주는" 구조이므로 **새 시스템을 만들지 않는다** — 발주자를 익명의
    게시판에서 **픽서**로 승격하고 문구·라벨만 바꾼다. 판정 코드는 그대로다.
  - **픽서는 이미 구출 대상 명단에 있다.** `ShelterNpcRoster`의 **연락책**(구출하면 의뢰 게시판이
    열린다)이 그 자리다 — 갇힌 연락책을 빼내면 일감이 들어오기 시작한다는 흐름이 그대로 성립한다.
  - **세력은 아직 배경이다.** 평판·세력간 배타·세력별 보상표는 **도입하지 않는다** — 실플레이
    리포트 전에 새 진행 축을 얹으면 밸런스 비교 기준을 잃는다(§11 「한 판 길이」와 같은 이유).
    지금 세력이 하는 일은 **드레싱과 적 소속의 설명**뿐이고, 확장 여지는 §11에 남긴다.

### 10.1 첫 던전 — 폐 아케이드 복합타워 (상승) · v0.3.3

- **왜 건물이어야 하는가 (v0.3.2의 근거는 그대로다)**: v0.3 §2가 수직성 어휘를 "계단실·붕괴 구멍·
  **창문**·엘리베이터 통로"로 재정의했는데, **이 넷이 전부 성립하는 것은 건물**이다. 특히 §5.2의
  **외벽 창문**(깨면 낙하로 이어지는 수평 시야 포털)은 지하 공간에서 성립하지 않는다.
  v0.3.3의 폐병원 → 아케이드 타워 재설정 당시에는 이 구조 근거를 건드리지 않았다 — **상승형 고층 건물이라는
  토폴로지는 불변**이고, 바뀐 것은 건물의 정체성과 드레싱 어휘였다.
- **왜 아케이드 복합타워인가**: 사이버펑크 어휘(네온 간판·자판기·홀로 패널·전광판)가 **가장 자연스럽게
  밀집하는 건물**이다. 병원은 위생 타일·수술등 등 어휘가 네온과 겉돌았다. 상가는 층마다 다른 점포로
  드레싱 변주가 무한하고, JOGAQ 레퍼런스(바·식당·자판기 골목)와 직접 정합한다.
- **구조**: 지하에서 시작해 **위로 올라간다**. `B2 → B1 → 1F → … → 8F(보스) → 옥상(출구)`.
  지하(주차장·전기실·기계실)와 지상(1F~ 상점가·아케이드, 중층 오피스·주거)의 공간 성격이 달라
  같은 던전 안에서 밀도 변주를 준다. 옥상 출구는 익스트랙션의 관용적 이미지(구조 신호)와도 맞는다.
  코드 ID `forgotten-catacombs`와 보스 표시명 `감시자`는 유지한다(리스킨 표 §0-a 관례).
- **첫 던전 적군 정체성**: 화면에서 읽히는 주체는 **점거군 돌격병·기업 보안 사수·기업 진압 로봇·
  기업 추적 드론·합선 검사 드론·사이버사이코 집행관**이다. `감시자`는 마지막 집행관의 현장 호칭이다.
  기업 추적 드론(호환 코드 ID `Slime`)은 낮은 사족 보안 드론으로, 쐐기형 센서 헤드·노출 서보 척추·
  손상된 군중제압제 카트리지의 제압제 주입턱을 가진다. 약하고 흔하게 넓게 배회하며, 근접 명중 시
  기존 중독 2턴을 부여하고 저상 기계라 사다리를 오르지 못한다.
  괴물·균류·초자연 생물처럼 보이는 실루엣은 쓰지 않는다. 전투 역할과 코드 ID는 보존하되,
  인간형 감시자는 실루엣 규칙에 맞춰 `CanClimb=true`로 둔다. 현 보스 아레나에는 사다리가 없어
  현재 조우 동선은 바뀌지 않으며, 세력 평판이나 새 전투 시스템을 추가하지 않는다.
- **층 내부는 단일 이동 평면이다.** 모든 일반 이동 칸은 `LocalHeight 0`이며 층내 `Stairs`·층내
  `Ladder`·캐치워크를 절차 생성하지 않는다. 전술적으로 중요한 높은 공간은 애매한 1~2단 단차가 아니라
  실제 인접 층으로 승격한다. 벽·간판·기계·천장 구조물은 이동 불가 건축 볼륨으로 높게 쌓을 수 있다.
- **층 수는 10으로 유지한다(잠정).** 깊이 기반 규칙(휴식처·탈출구·장비 드랍·숨은 방·밴드 경계)이
  10층 기준으로 조율돼 있어, 층 수를 먼저 늘리면 실플레이 리포트와 비교 기준을 잃는다.
  확장 여부는 §11 「한 판 길이」와 같이 **리포트를 본 뒤** 정한다.
- **낙하의 의미가 뒤집힌다** — §5.3 참조. 이것이 이 결정의 실제 대가다.
- **용어 규약**: 진행 지수(내부)와 층 라벨(표시)을 분리한다. 진행 구간 이름은 방향 중립어
  (**초반 / 중반 / 후반 / 보스**)를 쓰고, `Shallow/Mid/Deep` 같은 깊이 어휘는 쓰지 않는다.
  상승·하강·평면 던전이 같은 구간 롤업을 공유해야 리포트를 나란히 비교할 수 있다.

### 10.2 두 번째 원정지 — 침수된 금고 (진입 깊이)

- **왜 두 번째가 상승도 하강도 아닌가**: 아케이드 타워가 증명한 것은 "방향은 던전별 데이터다"까지다.
  거기서 멈추면 기둥 1이 결국 **위냐 아래냐** 한 축으로 굳는다. 침수된 금고는 **고도가 진행 축이 아니다** —
  얼마나 안쪽으로 파고들었는가가 진행이고, 오르내림은 층 안의 국소 지형일 뿐이다
  (`DungeonProgressDirection.Inward`). 기둥 1은 높이가 아니라 **공간 전체**를 전술 자원으로 쓴다는
  뜻이며, 그 주장이 말뿐이 아님을 이 던전이 증명한다.
- **구조**: 10개 구역. 층 라벨을 쓰지 않고 **`1구역 → 10구역`** 으로 표기한다. 층을 쌓는 것은
  렌더·컬링을 위한 **엔진 사정**이지 플레이어가 보는 사실이 아니어서, `B1`/`1F` 라벨은 화면에서
  거짓이 된다(§10.1 용어 규약의 연장).
- **보스가 없다 — 출구는 처음부터 열려 있다.** 아케이드 타워의 옥상 봉인과 정반대다. 완주의 무게가
  문지기 하나가 아니라 **누적된 소모**에서 나오는 판이 하나는 있어야, 익스트랙션의 "언제 돌아설
  것인가"가 보스 유무와 무관한 판단이라는 게 드러난다.
- **무대는 물이다**(지역 프로파일 `Flooded`): 웅덩이 확률이 크게 오르고 광원은 더 드물다 —
  물에 죽은 비상등. **높이를 뺀 자리를 기둥 2(상태이상·요소 반응)가 채운다** — 얼려서 발판을 만들고,
  통전된 웅덩이를 피하거나 역이용하는 것이 이 던전의 문법이다. 드롭 편성도 지역이 가른다.
- **전용 적 — 합선 검사 드론**: 물 위에서만 만나는 원거리 적. 직접 화력은 낮지만 명중점 주변과
  **이어진 웅덩이 전체를 통전**시켜 물 위의 안전한 장거리 교환을 깨뜨린다. 기계라 사다리를 못 탄다.
  "던전의 정체성은 층 수나 seed가 아니라 **다른 적**"이라는 §11의 선행 조건을 이 한 종이 충족한다.
- 엘리베이터(§5.3)는 놓지 않는다 — 오르내림이 진행이 아니라고 선언한 던전에서 층을 관통하는
  승강 연출은 화면과 어긋난다.

### 이 결정들이 파생시키는 것 (추후 설계 항목)

- 메타 프로그레션 해금 축은 **§11에서 확정됐다** — 조건 달성 도구 해금 · NPC 구출 시설 · 골드 · 기록.
  영구 스탯 강화와 역할/직업 해금은 채택하지 않는다(더 이상 열린 질문이 아니다).
- 첫 던전은 **폐 아케이드 복합타워 10개 층(B2~8F) + 옥상 출구**. 최상층 보스(역할=문지기,
  표시명 `감시자`)를 잡기 전까지 옥상 출구는 봉인된다.
- 보스/최심층 보상 수치는 실제 플레이 데이터로 조정한다. **완주 시간에는 목표치를 두지 않는다** —
  밀도에서 파생되는 값으로 본다(§11 「한 판 길이」).
- 세력 구도 → 몬스터/아이템 로스터를 리스킨 표 기준으로 확정(적의 **소속**을 실루엣으로 읽히게)

## 11. 아직 안 정한 것 (Open Questions)

주석에서 "다루자"로 표시됐던 항목들. **아래 넷 중 셋은 이미 닫혔고**(제목의 확정 표시로 구분한다)
남은 것은 후속 원정지뿐이다. 닫힌 항목을 여기 남겨 두는 이유는 결론이 아니라 **왜 그렇게 됐는가**가
다음 결정의 근거이기 때문이다 — 결론만 옮기면 같은 논쟁이 반년 뒤에 처음부터 다시 열린다.

- **메타 프로그레션 해금 대상 — 확정(v0.3.2)** → 제안: ① 조합 레시피, ② 시작 장비/아이템. 영구 스탯 강화는 로그라이트 밸런스를 무너뜨리기 쉬워 신중.
  ③ 직업 해금은 **없앴다** — 직업제를 채택하지 않기로 확정했다(아래 "단일 영웅 vs 직업 선택").
  현재 구현된 축은 **조건 달성 도구 해금 + NPC 구출 시설 해금 + 골드**이고, 여기에 **기록**을 더했다:
  판이 끝날 때마다 적립돼 기록실에서 조건에 투입하는 값이다. 죽음이 남기는 것은 물자도 능력치도
  아니어야 하므로(소지품 전손 = 익스트랙션의 긴장, 영구 스탯 강화 = 금지) **늘어나는 것은 숫자가
  아니라 선택지**다. 상세는 `docs/SYSTEMS.md` "해금 · 기록".
- **한 판 길이 — 확정(v0.3.1)** → **시간은 목표가 아니라 결과다.** 밀도(방 크기·적 배치·상호작용
  거리)를 먼저 설계하고, 한 판 길이는 거기서 나오는 값으로 받는다. **밀도가 높으면 1시간도 정상**이다.
  이전 문구("20~30분 목표에 맞춰 밀도를 조정")는 인과가 거꾸로였다 — 시간에 맞추려고 밀도를 깎으면
  파밍(기둥 4)과 탐색이 먼저 얇아진다. 층 체크포인트·이어하기가 있어 긴 판이 한 세션에 묶이지도 않는다.
  리포트의 소요 시간은 **"길다/짧다"의 판정이 아니라 밀도가 실제로 얼마였는지의 지표**로 읽는다.
- **단일 영웅 vs 직업 선택 — 확정(v0.3.2)** → **단일 원정자다.** 직업도 프리셋도 두지 않는다.
  이 게임은 익스트랙션 모델이고 스킬 트리가 없다 — 정체성을 지는 것은 캐릭터가 아니라 **장비**이며,
  장비는 이미 "숫자가 아니라 규칙을 바꾼다"로 설계돼 있다(§5.10 장비). 시험 삼아 둔 영웅 3종은
  HP/공격력 **숫자만** 달라, 고르는 행위가 전술이 아니라 **난이도 선택**에 가까웠다.
  무엇을 걸고 나갈지가 판돈인 게임에서 캐릭터를 먼저 고르게 할 이유가 없다.
  기본 수치는 `SurvivorProfile` 하나가 소유한다(직업제 재도입은 이 결정을 뒤집어야 하는 사안이다).
- **세 번째 이후 원정지와 던전 체인 — 미정** → 아케이드 타워(§10.1)·침수된 금고(§10.2) 두 곳의 리포트를
  기준으로 체인/생환 지점을 결정한다. 방향 자체는 더 이상 열린 질문이 아니다:
  **층 수와 방향은 던전별 정체성 축이며(v0.3.2)** 전역 스위치가 아니라 던전마다 선언하는 값이다
  (`DungeonProgressDirection`). 세 가지가 실제로 공존한다:
  - **하강**이 주 목적인 던전(지하 시설), **상승**이 주 목적인 던전(아케이드 타워 — §10.1).
  - **고도가 축이 아닌 던전** — "얼마나 깊이 들어왔는가"가 진행이고 오르내림은 국소 지형일 뿐이다.
    기둥 1이 높이로만 표현되는 게 아니라 **공간 전체**로 표현된다는 뜻이며, 이런 던전은
    높이 대신 상태이상·요소 반응(기둥 2)을 무대로 삼는다 — 두 기둥이 대등하므로(§2) 성립하는
    설계이며, **침수된 금고로 실현됐다(§10.2).**
  같은 seed로 층 수만 바꾼 던전은 정체성이 아니다 — 선행 조건은 언제나 "다른 적"이다.

## 12. PC / 모바일 크로스플랫폼 고려사항 (신규)

> 둘 다 지원하려면 처음부터 **입력과 UI를 추상화**해야 나중에 안 갈아엎는다.
> 현재는 개발 속도를 위해 PC 가로만 검증한다. 모바일 레이아웃·입력 추상화는 삭제하지 않지만
> 새 기능의 패리티 대상에서는 임시로 제외한다(`docs/UI_ARCHITECTURE.md` 참조).

- **입력 추상화**: "탭 = 클릭", "롱프레스 = 우클릭/호버" 등을 게임 로직이 아닌 **입력 레이어**에서 통일. Unity **Input System(new)** 사용 → 터치/마우스/키보드/게임패드를 액션 단위로 매핑.
- **호버 부재 문제**: PC는 마우스 호버로 정보 툴팁을 주지만 모바일엔 호버가 없음 → **탭=선택, 재탭=실행** 2단계 또는 롱프레스로 정보 표시.
- **UI 스케일/해상도**: Canvas Scaler로 다양한 화면비(모바일 세로/가로, PC 와이드) 대응. 안전영역(노치) 처리.
- **정밀 조준**: 마우스는 픽셀 단위, 터치는 손가락이 굵음 → **타일 단위 스냅 + 확인 단계**로 두 입력 모두 커버 (턴제라 유리).
- **성능 예산**: 모바일이 하한선 → 몬스터 컬링(§5.7)·렌더 층 제한이 여기서도 중요.
- **저장/동기화**: 추후 크로스 세이브 고려 시 저장 포맷을 플랫폼 독립적으로.

---

*다음 액션과 현재 구현 상태는 `docs/ROADMAP.md`와 `docs/STATUS.md`를 따른다.*
