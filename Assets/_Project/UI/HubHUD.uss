.hub-topbar {
    position: absolute;
    top: 8px;
    left: 8px;
    right: 8px;
    flex-direction: row;
    justify-content: space-between;
}

.hub-pill {
    flex-direction: row;
    align-items: center;
    padding: 2px 6px;
}

.hub-top-actions {
    flex-direction: row;
    align-items: center;
}

.hub-caption {
    color: var(--pc-dim);
    font-size: var(--pc-fs-1);
    margin-right: 8px;
}

.hub-gold {
    color: rgb(255, 213, 84);
    font-size: var(--pc-fs-2);
}

.hub-continue {
    color: rgb(238, 244, 230);
    font-size: var(--pc-fs-1);
    display: none;
}

.hub-continue.is-available {
    display: flex;
}

.hub-icon-button {
    width: 20px;
    height: 20px;
    margin-left: 4px;
    color: var(--pc-gold);
    font-size: var(--pc-fs-1);
}

/* DesignSystem 의 .ui-icon 기본값은 24px(옛 960×540 캔버스)이고 허브엔 오버라이드가
   없었다 — "갤러리 전용"이라던 옛 기본값이 라이브 화면으로 새던 유일한 경로다. */
.hub-icon-button .ui-icon {
    width: 10px;
    height: 10px;
    min-width: 10px;
    min-height: 10px;
}

.hub-menu-actions {
    align-self: stretch;
    justify-content: center;
}

.hub-menu-actions > .settings-reset,
.hub-menu-actions > .settings-done {
    width: 80px;
}

/* 던전 메시지 로그와 같은 플레이트 어휘. 캠프도 같은 시스템으로 읽혀야 한다. */
.hub-status-zone {
    position: absolute;
    bottom: 8px;
    left: 8px;
    right: auto;
    translate: 0 0;
    align-items: flex-start;
}

/* 상시 문장이 아니다 — 컨트롤러가 켜고 7초 뒤 닫는다. */
.hub-status-zone .feedback-chip {
    display: none;
    min-height: 0;
    max-width: 264px;
    padding: 2px 4px;
    margin-top: 0;
    border-radius: 0;
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-left-color: var(--pc-stone-dim);
    border-right-color: var(--pc-stone-dim);
    border-top-color: var(--pc-stone-dim);
    border-bottom-color: var(--pc-stone-dim);
    background-color: rgba(5, 7, 12, 0.78);
}

.hub-status-zone .feedback-chip.is-open {
    display: flex;
}

.hub-status-zone .chip-dot {
    display: none;
}

.hub-status-zone .chip-label {
    color: var(--pc-text);
    font-size: var(--pc-fs-1);
    white-space: normal;
}

.hub-dungeon-card {
    width: 528px;
    max-width: 92%;
}

.hub-dungeon-layout {
    flex-direction: row;
    margin-top: 8px;
    margin-bottom: 8px;
}

.hub-dungeon-list {
    width: 42%;
    margin-right: 8px;
}

.hub-dungeon-option {
    min-height: 32px;
    margin-bottom: 4px;
    padding: 4px 6px;
    color: var(--pc-text);
    background-color: var(--pc-btn-bg);
    border-width: 1px;
    border-color: var(--pc-stone);
    white-space: normal;
    -unity-text-align: middle-left;
}

.hub-dungeon-option.selected {
    color: var(--pc-gold);
    background-color: var(--pc-action-bg);
    border-color: var(--pc-gold-deep);
}

/* 잠금 표현은 `locked` 한 벌이다. 컨트롤러가 `is-locked` 를 토글하던 시절엔 USS 어디에도
   그 규칙이 없어 붙어도 화면이 변하지 않았다(UI_ARCHITECTURE 의 "알려진 어긋남"). */
.hub-dungeon-option.locked,
.hub-bounty-row.locked {
    color: var(--pc-dim);
    opacity: 0.55;
}

.hub-dungeon-detail {
    flex-grow: 1;
    min-height: 104px;
    padding: 8px;
    background-color: rgba(10, 12, 16, 0.72);
    border-left-width: 2px;
    border-left-color: var(--pc-gold-deep);
}

.hub-dungeon-name {
    color: var(--pc-gold);
    font-size: var(--pc-fs-2);
}

.hub-dungeon-route {
    color: var(--pc-teal);
    font-size: var(--pc-fs-1);
    margin-top: 8px;
}

.hub-dungeon-desc {
    color: var(--pc-text);
    font-size: var(--pc-fs-1);
    margin-top: 6px;
    white-space: normal;
}

.hub-dungeon-legend {
    color: var(--pc-dim);
    font-size: var(--pc-fs-1);
    margin-top: 8px;
    white-space: normal;
}

.hub-dungeon-enter {
    min-width: 120px;
}

.hub-shop-list {
    flex-direction: row;
    flex-wrap: wrap;
    margin-bottom: 8px;
}

.hub-shop-item {
    width: 31%;
    margin: 1%;
    padding: 4px 2px;
    font-size: var(--pc-fs-1);
    background-color: rgb(34, 28, 18);
    color: rgb(233, 220, 192);
    border-radius: 6px;
    border-width: 1px;
    border-color: rgb(74, 58, 34);
    white-space: normal;
}

.hub-shop-item:hover {
    background-color: rgb(50, 40, 26);
}

.hub-feedback {
    color: rgb(255, 213, 84);
    font-size: var(--pc-fs-1);
    min-height: 12px;
}

.hub-stash-detail {
    white-space: normal;
}

/* ── 상점/창고 슬롯 그리드 ───────────────────────────── */

.hub-header-right {
    flex-direction: row;
    align-items: center;
}

.hub-modal-gold {
    color: rgb(255, 213, 84);
    font-size: var(--pc-fs-2);
    margin-right: 10px;
}

.hub-shop-feedback {
    flex-grow: 1;
    -unity-text-align: middle-left;
}

.slot-price {
    color: rgb(255, 213, 84);
    font-size: var(--pc-fs-1);
}

/* 허브도 던전과 동일한 Torchstone 팔레트/모서리/강조 규칙을 사용한다. */
.hub-caption {
    color: var(--pc-dim);
    font-size: var(--pc-fs-1);
}

.hub-gold,
.hub-modal-gold,
.hub-feedback,
.slot-price {
    color: var(--pc-gold);
}

.hub-continue {
    color: var(--pc-text);
    font-size: var(--pc-fs-1);
}

.hub-shop-item {
    color: var(--pc-text);
    background-color: var(--pc-btn-bg);
    border-left-color: var(--pc-stone);
    border-top-color: var(--pc-stone);
    border-right-color: var(--pc-stone-dim);
    border-bottom-color: var(--pc-stone-dim);
    border-radius: 0;
}

.hub-shop-item:hover {
    color: var(--pc-gold);
    background-color: var(--pc-action-bg);
    border-left-color: var(--pc-gold-deep);
    border-top-color: var(--pc-gold-deep);
}

.hud-root.is-narrow .hub-topbar {
    top: 8px;
    left: 8px;
    right: 8px;
}

.hud-root.is-narrow .hub-pill {
    padding: 5px 8px;
}

.hud-root.is-narrow .hub-icon-button {
    width: 44px;
    height: 44px;
}

.hud-root.is-narrow .hub-status-zone {
    left: 8px;
    right: 8px;
    bottom: 8px;
}

.hub-icon-button {
    align-items: center;
    justify-content: center;
}

.hud-root.is-narrow .hub-status-zone .status-chip {
    max-width: 100%;
}

.hud-root.is-narrow .hub-dungeon-card {
    max-width: 94%;
}

.hud-root.is-narrow .hub-dungeon-layout {
    flex-direction: column;
}

.hud-root.is-narrow .hub-dungeon-list {
    width: 100%;
    margin-right: 0;
    flex-direction: row;
}

.hud-root.is-narrow .hub-dungeon-option {
    flex-grow: 1;
    width: 31%;
    min-height: 58px;
    margin-right: 4px;
    padding: 6px;
    font-size: var(--pc-fs-1);
    -unity-text-align: middle-center;
}

.hud-root.is-narrow .hub-dungeon-detail {
    min-height: 148px;
    margin-top: 8px;
    padding: 12px;
}

/* ── 출정 준비: 창고 ↔ 멀티슬롯 백팩 ─────────────────── */

.expedition-card,
.hud-root.is-expanded .expedition-card,
.hud-root.is-landscape .expedition-card {
    width: 600px;
    max-width: 96%;
    /* 높이는 내용이 정하고 뷰포트가 상한을 준다. 88% 로 못박으면 창고가 비었을 때도
       카드가 화면을 채우며 페인이 늘어나고 하단 안내가 카드 밖으로 잘린다
       (인벤토리 카드에서 배운 것과 같다). */
    height: auto;
    max-height: 100%;
}

.expedition-layout {
    width: 100%;
    flex-direction: row;
    align-items: flex-start;
    justify-content: center;
}

/* 「짧은 화면」 정책(docs/UI_ARCHITECTURE.md): 컨트롤을 축소하지 않고 본문만 스크롤한다.
   본문 = 3열 배치뿐이다. 헤더와 상세/이동 버튼 줄은 카드에 고정된다. */
.expedition-body-scroll {
    flex-grow: 1;
    flex-shrink: 1;
    min-height: 0;
}

/* 창고 격자는 백팩 격자와 같은 160px 창으로 잘라 두 페인의 눈높이를 맞춘다.
   48칸은 5열 × 10줄이라 어떤 배치로도 360 캔버스에 다 들어가지 않는다 — 스크롤이 정답이고,
   스크롤은 격자 안에서 끝나야 ↔ 레일과 드롭 대상(백팩)이 항상 화면에 남는다. */
.expedition-stash-scroll {
    height: 160px;
    min-height: 0;
    flex-shrink: 0;
}

.expedition-card .unity-scroll-view__horizontal-scroller {
    display: none;
}

.expedition-card .unity-scroll-view__content-viewport {
    overflow: hidden;
}

.expedition-pane {
    padding: 6px;
    background-color: rgb(8, 10, 14);
    border-left-width: 1px;
    border-right-width: 1px;
    border-top-width: 1px;
    border-bottom-width: 1px;
    border-left-color: var(--pc-stone-dim);
    border-top-color: var(--pc-stone-dim);
    border-right-color: rgb(30, 27, 25);
    border-bottom-color: rgb(30, 27, 25);
}

.expedition-stash-pane {
    width: 272px;
}

.expedition-loadout-pane {
    width: 256px;
    min-height: 148px;
}

.expedition-transfer-rail {
    width: 48px;
    min-height: 96px;
    align-items: center;
    justify-content: center;
}

.expedition-transfer-symbol {
    color: var(--pc-gold);
    font-size: var(--pc-fs-2);
}

.expedition-transfer-hint,
.expedition-hero-label {
    color: var(--pc-dim);
    font-size: var(--pc-fs-1);
    white-space: normal;
    -unity-text-align: middle-center;
}

.expedition-transfer-hint {
    margin-top: 8px;
}

/* 한 줄 헤더의 가운데 칸이다 — 줄바꿈을 허용하면 헤더가 높아져 격자를 밀어낸다. */
.expedition-hero-label {
    flex-shrink: 1;
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
}

.expedition-backpack-grid {
    flex-shrink: 0;
}

/* `height: auto` 는 제목 아래 캡션을 두 줄로 쌓던 시절의 규칙이었다. 캡션이 헤더 한 줄로
   들어온 지금은 페인만 키우므로 지웠다 — 높이는 아래 `.expedition-card` 규칙이 정한다. */
.hud-root.is-expanded .expedition-loadout-pane .inventory-section-header,
.hud-root.is-landscape .expedition-loadout-pane .inventory-section-header {
    width: 100%;
}

.hud-root.is-expanded .expedition-loadout-pane .backpack-grid,
.hud-root.is-landscape .expedition-loadout-pane .backpack-grid {
    width: 240px;
    min-width: 240px;
}

.hud-root.is-expanded .expedition-card .unity-scroll-view__content-container,
.hud-root.is-landscape .expedition-card .unity-scroll-view__content-container {
    padding-right: 8px;
}

/* 카드에 고정되는 줄이므로 절대 줄어들면 안 된다 — 여기가 눌리면 이동 버튼이 다시 사라진다. */
.expedition-detail {
    width: 100%;
    min-height: 56px;
    margin-top: 6px;
    padding: 4px 6px;
    flex-shrink: 0;
    flex-direction: row;
    align-items: center;
}

.expedition-detail-copy {
    flex-grow: 1;
    min-width: 140px;
}

/* 고정 줄의 세로 여백은 그대로 본문에서 빼앗은 것이다 — 360 캔버스에서는 그 몇 px 이
   백팩 격자의 마지막 줄이라, 글자 크기는 두고 줄간만 좁힌다. */
.expedition-feedback {
    margin-top: 2px;
}

.expedition-detail .inventory-detail-name {
    margin-bottom: 0;
}

/* `.hud-root.is-short .settings-header`(0,3,0) 를 이겨야 한다 — 짧은 화면 기본값 36px 을
   그대로 두면 아래에서 아무리 짜내도 백팩 격자가 들어오지 않는다. 닫기 버튼 16px 은 그대로다. */
.hud-root.is-short .expedition-card > .settings-header {
    height: 30px;
    margin-bottom: 2px;
}

/* 두 페인의 제목 줄. `.hud-root.is-landscape …`(0,3,0) 와 같은 특정성이어야 이긴다. */
.hud-root .expedition-card .inventory-section-header {
    height: 20px;
}

/* `.hud-root.is-short .inventory-grid`(0,3,0) 의 4px 아래 여백을 끈다 — 페인 두 개에서
   각각 4px 이면 백팩 격자의 마지막 줄이 다시 접힌다. */
.hud-root.is-short .expedition-card .inventory-grid {
    margin-bottom: 0;
}

/* 버튼을 세로로 쌓으면 고정 줄이 64px 이 되어 본문에서 그만큼을 빼앗는다 — 360 캔버스에서는
   그 64px 이 백팩 격자의 마지막 줄이다. 가로로 눕히면 28px 이면 되고, 버튼도 넓어져
   "백팩에 넣기 (3회분) →" 같은 회분 표기가 잘리지 않는다. */
.expedition-actions {
    margin-left: 6px;
    flex-direction: row;
    flex-shrink: 0;
}

.expedition-transfer-button {
    width: 120px;
    min-height: 20px;
    margin: 0 2px;
}

.backpack-item.loadout-starter {
    background-color: rgb(19, 31, 36);
    border-left-color: var(--pc-teal);
    border-top-color: var(--pc-teal);
}

.loadout-starter-badge {
    position: absolute;
    left: 2px;
    top: 2px;
    color: var(--pc-ice);
    font-size: var(--pc-fs-1);
    background-color: rgb(6, 15, 19);
}

.expedition-drag-source {
    opacity: 0.5;
}

.expedition-pane.drop-valid {
    border-left-color: var(--pc-gold);
    border-right-color: var(--pc-gold);
    border-top-color: var(--pc-gold);
    border-bottom-color: var(--pc-gold);
    background-color: rgb(28, 24, 13);
}

.expedition-pane.drop-invalid {
    border-left-color: rgb(225, 76, 66);
    border-right-color: rgb(225, 76, 66);
    border-top-color: rgb(225, 76, 66);
    border-bottom-color: rgb(225, 76, 66);
    background-color: rgb(35, 12, 14);
}

.hud-root.is-narrow .expedition-card {
    width: 94%;
    max-width: 94%;
    height: 94%;
}

.hud-root.is-narrow .expedition-layout {
    flex-direction: column;
    align-items: center;
}

.hud-root.is-narrow .expedition-stash-pane,
.hud-root.is-narrow .expedition-loadout-pane {
    width: 100%;
    max-width: 100%;
}

.hud-root.is-narrow .expedition-transfer-rail {
    width: 100%;
    min-height: 48px;
    flex-direction: row;
}

.hud-root.is-narrow .expedition-transfer-hint {
    margin: 0 0 0 8px;
}

.hud-root.is-narrow .expedition-detail {
    flex-direction: column;
    align-items: stretch;
}

.hud-root.is-narrow .expedition-detail .inventory-detail-icon {
    align-self: center;
}

.hud-root.is-narrow .expedition-actions {
    width: 100%;
    min-width: 0;
    margin: 10px 0 0 0;
    flex-direction: row;
}

.hud-root.is-narrow .expedition-transfer-button {
    width: 49%;
    margin: 0 2px;
    flex-grow: 1;
}

/* ── 대장간 / 의뢰 게시판 ─────────────────────────────── */

.hub-smith-list,
.hub-bounty-list {
    flex-direction: column;
    margin-top: 6px;
}

.hub-smith-row,
.hub-bounty-row {
    background-color: rgb(34, 28, 18);
    border-width: 1px;
    border-color: rgb(74, 58, 34);
    border-radius: 6px;
    padding: 8px 10px;
    margin-bottom: 6px;
}

.hub-smith-row {
    flex-direction: row;
    align-items: center;
}

.hub-smith-info {
    flex-grow: 1;
    flex-shrink: 1;
}

.hub-row-title {
    color: var(--pc-text);
    font-size: var(--pc-fs-1);
}

.hub-row-desc {
    color: var(--pc-dim);
    font-size: var(--pc-fs-1);
    white-space: normal;
    margin-top: 2px;
}

.hub-smith-buy {
    min-width: 56px;
    margin-left: 6px;
}

.hub-smith-buy:disabled {
    opacity: 0.5;
}

.hub-bounty-row {
    flex-direction: column;
}

.hub-bounty-reward {
    color: var(--pc-gold);
    font-size: var(--pc-fs-1);
    margin-top: 3px;
}

/* 기록 투입 버튼. 줄 안에서 왼쪽에 붙여 진행 문구 바로 아래에 오게 한다 —
   눌렀을 때 어떤 숫자가 움직이는지가 시선 이동 없이 읽혀야 한다. */
.hub-codex-invest {
    align-self: flex-start;
    min-width: 64px;
    margin-top: 4px;
}

.hub-codex-invest:disabled {
    opacity: 0.5;
}

.hub-bounty-empty {
    color: var(--pc-dim);
    font-size: var(--pc-fs-1);
    white-space: normal;
}

/* 던전 모달과 같은 창 어휘. DesignSystem 의 .pc-window 는 화면별 USS 보다 먼저 로드돼
   같은 특정성에서 밀리므로, .settings-card 의 솔리드 패널과 보더를 여기서 걷어내야
   9-slice 프레임이 보인다(PrototypeHUD.Desktop.uss 와 같은 처리). */
.hud-root .settings-card.pc-window {
    background-color: rgba(0, 0, 0, 0);
    border-left-width: 0;
    border-right-width: 0;
    border-top-width: 0;
    border-bottom-width: 0;
    padding: 12px;
}
